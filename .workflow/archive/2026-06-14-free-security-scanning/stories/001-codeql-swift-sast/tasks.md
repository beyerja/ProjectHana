## Tasks
- [x] 001: Author `.github/workflows/codeql.yml` — Swift analysis on macos-15, triggers (PR to main, push to main, weekly schedule), github/codeql-action@v4 init/build/analyze, security-events: write, build via project toolchain (no /nix paths)
- [x] 002: Validate the workflow YAML locally (parse/lint) and confirm action pinned to latest verified major (v4)
