## Tasks

- [x] 001: Write `.claude/agents/triage-dep-prs.md` — the complete agent file with frontmatter, telemetry bookends, PR detection logic (gh pr list jq filter for bot-authored PRs with `dependencies` label or `dependabot/`/`renovate/` branch prefix), no-dep-PRs early exit, and per-PR state machine covering: (a) clean PR → lint+test → post code-owner-review check via `scripts/gh-review-bot.sh` → verify app.id=4144849 → squash-merge; (b) merge conflict → fetch branch → rebase onto main preserving Package.swift/Package.resolved version bumps → force-push → re-enter CI verification; (c) test failure after rebase → attempt minimal call-site fix → commit via file + `git commit -F` → push → re-enter CI verification; (d) cannot auto-fix → append PR URL+reason to `.workflow/log.md` → continue to next PR; plus CI self-heal (close/reopen to re-trigger if required contexts absent on head SHA), graceful degradation (wrapper exits non-zero → plain gh comment + record SKIPPED), and final STATUS output listing skipped PRs — all git ops use `-C <path>`, no heredocs, no `cd &&` compounds

- [x] 002: Update `.claude/agents/feature_orchestrator.md` — add Step 1 (spawn `triage-dep-prs` agent, passing the repo and worktree path) immediately after Step 0 (worktree setup) and before `clarify-feature`; add a follow-on git pull step (`git -C <worktree> fetch origin && git -C <worktree> merge origin/main`) after triage completes to pull freshly-merged dep changes into the worktree branch before any story work begins
