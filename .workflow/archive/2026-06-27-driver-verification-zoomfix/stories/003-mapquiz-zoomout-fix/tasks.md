## Tasks

- [x] 001: Relax the zoom-out cap in `QuizRegionMath.cameraBounds` (Hanahuac/Views/Quiz/MapQuiz/MapQuizRegionHelper.swift). Raise `cameraDistanceHeadroom` from 1.15 to a value large enough to let the camera zoom OUT to a continental/world view (e.g. ~12–20x), and/or pass an explicitly relaxed `maximumDistance` to `MapCameraBounds`. Keep `centerCoordinateBounds: region` unchanged so the INITIAL framing stays pinned to the candidate-pin region; do NOT add `minimumDistance` re-framing or any overlay-driven re-framing of the initial camera. Update the doc-comments on `cameraDistanceHeadroom`/`cameraBounds`/`cameraDistance(for:)` to reflect that the cap now intentionally permits continental zoom-out (the old comments claim it hard-caps far below overlay extent — that intent is being reversed for zoom-out range only).

- [x] 002: Confirm both consumers inherit the relaxed range with no per-call override. Verify MapQuizView.swift:64 and MapLearningQuizView.swift:86 both call `QuizRegionMath.cameraBounds(for: session.mapRegion)` unchanged (the relaxation lives entirely in the shared helper, applying to all map-quiz categories by default). No code change expected unless a view passes its own bounds; if so, route it through the shared helper. Record the verification in the story log.

- [x] 003: Update `MapQuizRegionHelperTests` (HanahuacTests/MapQuizRegionHelperTests.swift) for the new zoom-out range. The existing tests `testCameraDistanceCapStaysAtCandidatePinScaleNotOverlayScale` and `testCameraDistanceFramesEveryCandidatePin` assert the cap stays BELOW overlay scale (`cap < 60° overlay extent`) — these now contradict AC5 and must be revised: assert the relaxed `maximumDistance` (= `cameraDistance(for:) * cameraDistanceHeadroom`) is now LARGE enough for continental/world zoom-out (e.g. exceeds the overlay extent / a continental-span threshold). Add an assertion that `minimumDistance` and the framed initial `region` (center = bounding-box center, span derived from all pins) are UNCHANGED — `region(fittingPins:)` containment tests must still pass. Keep `testCameraBoundsCenterRegionIsBoundingBoxNotCorrectPin` (no answer-pin hint leak) green.

- [x] 004: Run `just lint` and `just test`; fix any failures so the MapQuizRegionHelper changes and revised tests pass.

- [x] 005: Author the AC6 action script `.workflow/ui-walkthrough/scripts/006-mapquiz-zoomout.json` that drives the COUNTRY map quiz and zooms OUT. Steps: dumpTree + screenshot on home; tap `home.mode.mapQuiz` (country section is first) to enter the country map quiz; wait + dumpTree + screenshot (baseline framing); `pinch` with `scale` < 1 (zoom out — see `UIActionKind.pinch` / `UIActionStep.scale` convention in HanahuacUITests/UIActionScript.swift, where the driver derives a negative velocity from `scale < 1`); wait + dumpTree + screenshot (post-zoom-out). The before/after dumps + screenshots are the evidence that the visible map span/region grows.

- [x] 006: Run the script via `just ui-walkthrough script=".workflow/ui-walkthrough/scripts/006-mapquiz-zoomout.json"` on the iPhone 17 / iOS 26.5 simulator. Inspect the produced artifacts under `.workflow/ui-walkthrough/<run>/` (per-step screenshots + accessibility dumps) and confirm the visible map span/region is larger after the `pinch` zoom-out than before.

- [x] 007: Write the AC6 verify log: reference the concrete artifact paths (screenshots + dumps) from task 006 proving the zoom-out, in the story verify log / log.md. This is the end-to-end proof that the relaxed cap lets the user zoom out, captured via `just ui-walkthrough` + the new `pinch` action.
