## Tasks

- [x] 001: In `.claude/agents/verify-story.md`, rewrite the `## Visual Verification` section so `just ui-walkthrough` is the DEFAULT verification for any story touching `Hanahuac/Views/**` — trigger on the changed file path, not solely on an opt-in `## Visual Verification` section in the spec. Keep the existing single-screenshot path only as the documented sim-unavailable fallback. Direct the verifier to author a focused JSON action script under `.workflow/ui-walkthrough/scripts/<story-id>.json`, run `just ui-walkthrough <script> <story-id>`, then inspect EVERY per-step `NNN-step.png` AND `NNN-step.json` accessibility dump in the printed run dir (not one static screenshot). Reference `.workflow/ui-walkthrough/README.md` for the action-script schema. Preserve the section's existing numbered-step + pass/fail/loop-back structure and style.
- [x] 002: In `.claude/agents/verify-story.md`, add an explicit sim-unavailable fallback within the same section: if `just boot-sim`/`just install-sim` cannot run (no simulator), fall back to launch+single-screenshot (the prior behavior) or to inspecting unit-tested presentation logic, and record the verification method used — do not block the story loop on a missing simulator.
- [x] 003: In `.claude/agents/verify-feature.md`, replace the single static end-to-end screenshot in the `## Visual Verification` section with a broader multi-screen `just ui-walkthrough` walkthrough that NAVIGATES across the feature's affected flows (tap/scroll/mapTap steps spanning each affected screen) and inspects the per-step screenshots AND accessibility dumps. Keep the sim-unavailable / unit-tested-presentation fallback that already exists (the "No tap automation available" note becomes the fallback, not the primary path). Preserve the file's existing structure and the telemetry/note-string cautions.
- [x] 004: Add an AC3 issue-hunting directive to `.claude/agents/verify-story.md` (within or adjacent to the walkthrough section) enumerating the concrete bug classes to actively hunt for in the per-step artifacts: (a) an EMPTY accessibility tree in any `NNN-step.json` = app crashed / gone, hard fail; (b) English/untranslated strings while the UI locale is non-English; (c) duplicated, missing, obscured, or overlapping controls. Instruct the verifier to FAIL and loop back (output STATUS: FAILED with details so implement-story re-runs) when any is found — not merely confirm it "matches the spec."
- [x] 005: Add the same AC3 issue-hunting directive to `.claude/agents/verify-feature.md`, wired into its FAIL path (output STATUS: FAILED: <criterion> — story <id> so the responsible story is retried). Keep wording consistent with task 004 and consistent with verify-feature's existing per-criterion / per-story failure-attribution outcome format.
- [x] 006: Final consistency pass across both edited files: confirm changes are documentation/agent-definition edits only (no app code), the `just ui-walkthrough` invocation + script path + artifact-dir conventions match `scripts/ui-walkthrough.sh`, the justfile recipe, and `.workflow/ui-walkthrough/README.md` (including the story-001 `pinch` action), Bash snippets use allowlistable shapes per CLAUDE.md, and each file's `## Outcomes`/telemetry blocks remain intact.
</content>
</invoke>
