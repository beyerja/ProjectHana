**(non-blocking, doc precision)** "An explicitly supplied `velocity` is used as-is" is slightly inaccurate: an explicit `velocity: 0` is treated like nil and replaced by the scale-aware default (correctly, since `velocity: 0` would itself raise `NSInvalidArgumentException`). The inline code comment in `UIDriverTests.swift` already captures this. Consider tightening to "an explicitly supplied **non-zero** `velocity` is used as-is".
