## Tasks
- [x] 001: In `HanahuacUITests/UIActionScript.swift`, add a `pinch` case to the action-kind enum (note: the enum is named `UIActionKind`, not `UIActionType` as the spec says — extend the existing type).
- [x] 002: In `HanahuacUITests/UIActionScript.swift`, add `scale: Double?` (required for pinch) and `velocity: Double?` (optional) to `UIActionStep`, keeping them optional so the single `Codable` model decodes coherently with the existing optional fields (no custom `init(from:)` needed — the synthesized decoder treats new optionals as absent for other actions).
- [x] 003: In `HanahuacUITests/UIDriverTests.swift`, add a `case .pinch:` arm to the `perform(_:in:)` switch that dispatches to a new `pinch(_:in:)` helper.
- [x] 004: In `HanahuacUITests/UIDriverTests.swift`, implement `pinch(_:in:)`: resolve the target via `resolveElement(step, in: app)` falling back to the whole `app` (consistent with how `swipe` falls back to `app` and how `mapTap` targets the map by coordinate when there is no addressable element); guard on `step.scale` being present (skip — no crash, no failure — when absent so an unresolvable/unparameterized target is skipped); call `element.pinch(withScale: scale, velocity: velocity ?? 1)` so scale<1 zooms out and scale>1 zooms in; for a label/identifier-targeted element, wait via `waitForExistence(timeout: elementTimeout)` before pinching (matching `tap`/`typeText`) and skip if it never appears.
- [x] 005: Document the `pinch` action in `.workflow/ui-walkthrough/README.md`: add `scale` (required) and `velocity` (optional) rows to the Fields table, add a `pinch` row to the Supported actions table (scale<1 = zoom out, scale>1 = zoom in; skipped if target unresolvable; per-step artifacts still captured), and add an example step (e.g. `{ "action": "pinch", "scale": 0.5 }` for zoom out).
- [x] 006: Run `just lint` and fix any lint findings on the changed files (no pbxproj hand-edits; `HanahuacUITests/` edits need no `just generate`).
- [x] 007: Run `just test` and confirm the driver builds and the suite passes; fix any failures.
