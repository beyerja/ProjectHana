## Tasks
- [ ] 001: Define the shared feature-slug convention (env var `HANA_FEATURE_SLUG`) and update `implement-story.md` to create story branches as `story/${HANA_FEATURE_SLUG}/<NNN>-<slug>` when set, else legacy `story/<NNN>-<slug>`.
- [ ] 002: Namespace chore/closing-artifact branches in `merge-pr.md` (and any other agent that creates chore branches) by the same slug when set.
