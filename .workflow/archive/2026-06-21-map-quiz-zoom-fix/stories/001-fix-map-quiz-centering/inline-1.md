Non-blocking (test quality): two of the three new assertions are tautological against their own re-derivation of the formula. `framedSpanMeters(of:)` recomputes `cameraDistance(for:)` byte-for-byte, so `testCameraDistanceFramesEveryCandidatePin` asserts `distance + 1.0 >= distance` and the first assertion here asserts `cap <= cap + 1.0` — both can essentially never fail and add no regression coverage. The load-bearing assertion is `cap < overlayExtentMeters` below, which genuinely fails if the cap is loosened to overlay scale; consider keeping that one and dropping (or strengthening) the tautological pair. Note also that the original defect was view-level (`Map` missing `bounds:`), which `MapCameraBounds`' opaque API can't unit-test directly — so this is the best available proxy, just trimmed of the no-op checks.
