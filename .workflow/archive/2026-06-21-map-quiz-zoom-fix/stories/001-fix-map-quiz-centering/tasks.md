## Tasks
- [x] 001: Confirm the root cause in code before changing anything. Trace the camera path in both `MapQuizView.swift` and `MapLearningQuizView.swift`: `Map(position: $position)` is seeded with `.region(session.mapRegion)`, but the Map content builder also emits `featureOverlays(for:answerState:)`, which renders large sea/mountain `borderRings` polygons and full-course river `linePath` polylines (from `MapFeatureRendering.swift`) that extend FAR beyond the candidate-pin bounding box. Confirm or refute that SwiftUI `Map` frames the union of its content (overlay extent) rather than honoring the seeded `.region`, and whether the initial `position` assignment in `buildSession()` races first content layout. Explicitly rule out: region math bug (`QuizRegionMath` is correct/tested), lat/lon swap, and pin-coordinate/JSON data error (all confirmed sane). Country works because its `borderRings` are small/local. Document the confirmed mechanism — why river/mountain/sea mis-center while country does not — in `log.md` BEFORE implementing the fix.
- [x] 002: Implement the shared fix in the region/centering path so the `Map` camera frames the candidate-pin bounding box (`session.mapRegion`) and is NOT overridden by the larger overlay geometry. Apply the change in the shared logic so a single path serves all four categories and BOTH `MapQuizView.swift` and `MapLearningQuizView.swift` (e.g. constrain/anchor the camera to the region, or otherwise prevent overlay extent from re-framing — exact approach per the confirmed mechanism from task 001). The correct pin must NOT be centered, zoomed-to, or otherwise distinguished: framing stays derived from the full candidate-pin bounding box via `QuizRegionMath.region(fittingPins:)`. No gratuitous per-category branch. Keep zoom behaviour unchanged except where centering genuinely requires it. Keep country (the control) working.
- [x] 003: Add regression test coverage in `HanahuacTests/MapQuizRegionHelperTests.swift` (or a focused new test file) that would have caught this regression — e.g. construct river/sea/mountain candidate pins together with their large overlay geometry (`borderRings` / `linePath` extents) and assert the framing still contains every candidate pin and that the correct pin is not the region center. Keep the existing country assertions green.
- [x] 004: Run `just -f /Users/Private/Documents/Code/ProjectHana-worktrees/map-quiz-zoom-fix/justfile lint` and `just -f /Users/Private/Documents/Code/ProjectHana-worktrees/map-quiz-zoom-fix/justfile test`; fix any failures until both pass.
