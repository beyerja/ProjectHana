## Tasks
- [x] 001: Commit `.github/CODEOWNERS` with `* @Hanahuac-Bot` assigning the whole repo to the bot as the required code owner. Add a top-of-file comment noting that CODEOWNERS alone does not block merges — only branch protection enforces the gate — so committing it is safe mid-run.
- [x] 002: Create the committed branch-protection JSON request body file (e.g. `.github/branch-protection-main.json`) for the `gh api --input <file>` activation command: requires an approving review from the code owner on `main` (`required_pull_request_reviews.require_code_owner_reviews: true`, `required_approving_review_count: 1`), with enforce_admins / required_status_checks / restrictions fields set to safe explicit values. Allowlistable shape — no heredoc, no inline JSON.
- [x] 003: Add `.github/branch-protection.md` documenting the obligatory review gate: a single ready-to-run, copy-pasteable `gh api -X PUT repos/beyerja/ProjectHana/branches/main/protection --input .github/branch-protection-main.json` activation command (allowlistable shape; no `cd &&`, body via `--input <file>`), explicitly marked as the FINAL activation step to run ONLY AFTER this run's own PRs merge (bootstrapping-guard rationale: enabling it mid-run would deadlock the workflow on its own un-reviewed PRs), plus a note that committing CODEOWNERS is safe mid-run because only branch protection enforces the gate, and a deactivation/rollback note (`gh api -X DELETE …/protection`).
- [x] 004: Cross-reference the gate from `.workflow/README.md`: add a short stub section pointing to `.github/branch-protection.md` (activation command + bootstrapping guard, when/how to flip the gate on) and the bot CODEOWNERS, noting story 004 owns the fuller setup/rotation docs that expand this section.
