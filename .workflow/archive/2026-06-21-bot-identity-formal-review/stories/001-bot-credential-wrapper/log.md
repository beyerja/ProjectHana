# Log — Committed Keychain-backed gh-review-bot.sh wrapper and a secret-scanning pre-commit hook

2026-06-20 break-tasks: DONE, 11 tasks
2026-06-20 implement-story: DONE — all 11 tasks (wrapper + absent-item + no-args guard, secret-scan hook, install-hooks + just install-hooks, two token-free test harnesses + just test-bot-scripts, lint-sh clean, docs/bot-credentials.md + README pointer). No issues; hook self-block avoided via runtime-generated real-shaped tokens in tests + ghp_FAKE allowlist sentinel.
2026-06-20 create-pr: BLOCKED — story branch feat/001-bot-credential-wrapper pushed to origin; PR could not be created because base branch feat/bot-identity-formal-review does not exist on remote and pushing the shared integration branch is out of scope (denied by auto mode). PR body staged at .workflow/stories/001-bot-credential-wrapper/pr-body.md. Orchestrator must push the integration base branch, then re-run create-pr (or run gh pr create --base feat/bot-identity-formal-review --head feat/001-bot-credential-wrapper --body-file <body>).
2026-06-20 create-pr: DONE — https://github.com/beyerja/ProjectHana/pull/123
2026-06-20 independent-review: CHANGES_REQUESTED — secret-scan hook whole-line allowlist lets a real token through when a FAKE sentinel shares its line (confirmed exit 0); wrapper itself clean.
2026-06-20 implement-story: DONE — addressed all 4 PR #123 review threads; pushed 6534198; replied to comments 3447022156/293/349/435.
2026-06-20 implement-story (review feedback): DONE — addressed all 4 PR #123 review threads. BLOCKING: secret-scan hook now extracts tokens per-token (grep -Eo COMBINED_PAT) and filters only FAKE sentinels (the hand-rolled +-line loop folded into a single grep -E '^\+[^+]'); added regression test asserting a mixed real+FAKE line is REJECTED (exit 1, was exit 0). install-hooks.sh now backs up a pre-existing non-shim hook (timestamped + warning, idempotent via marker) and bakes the absolute hook path into the shim instead of git rev-parse --show-toplevel at run time. Extracted shared scripts/test-lib.sh (TMPROOT+trap, pass/fail, ok/ko) sourced by both test scripts; removed the dead note(). lint-sh clean; test-bot-scripts 13/13 green (incl. new mixed-line test). No Swift/Python touched -> just install skipped.
2026-06-20 independent-review: CHANGES_REQUESTED — round-1 fix verified (per-token grep -Eo + mixed real/FAKE regression test; install-hooks backup/marker/abs-path; shared test-lib). New blocking: install-hooks.sh writes to $GIT_DIR/hooks but repo sets core.hooksPath=.githooks, so the secret-scan hook never runs (silently inert) and the existing .githooks/pre-commit main-branch guard is not composed with; tests miss it (throwaway repos, no core.hooksPath). Acceptance "activate hook on fresh clone" unmet.
2026-06-20 independent-review: APPROVED — round-2 blocking finding resolved. .githooks/pre-commit (the hook git runs under core.hooksPath=.githooks) composes the main-branch guard with the secret scan (resolves scan via git rev-parse --show-toplevel, propagates exit via || exit $?); install-hooks.sh sets core.hooksPath=.githooks idempotently (no dead shim). 3 new regression tests drive REAL git commit in a repo wired like ProjectHana: planted token REJECTED (proves hook executes), clean non-main commit succeeds, direct-to-main blocked. test-bot-scripts 16/16, lint-sh clean. No real token in diff; fakes runtime-built + ghp_FAKE sentinel. No new blocking findings; no non-blocking nits.
2026-06-20 verify-story: DONE — all 9 acceptance criteria satisfied on feat/bot-identity-formal-review (merge 0b98483). Verified by reading the committed scripts and running the committed suites with no real token: lint-sh clean (11 scripts); test-bot-scripts 16/16 green (test-gh-review-bot 9/9, test-secret-scan-hook 7/7). Wrapper committed+executable (100755), reads service hana-review-bot, execs args via `exec env GH_TOKEN=… "$@"`, no set -x, no echo/printf/redirect of the token value (grep confirms only usage/error text), fails closed naming service+docs when item absent, arg-passthrough proven by stub. Secret-scan hook rejects ghp_/github_pat_ shapes incl. mixed real+FAKE line, allows ordinary+ghp_FAKE sentinel; composed with main-branch guard in .githooks/pre-commit and proven to fire via real git commit under core.hooksPath=.githooks. install-hooks.sh sets core.hooksPath idempotently; docs/bot-credentials.md present. No .workflow/stories.md on this branch to check off.
2026-06-20 implement-story: DONE — review round 2, commit 2b214e8 pushed. Fixed the silently-inert hook. .githooks/pre-commit (run by this repo's core.hooksPath=.githooks) now composes the main-branch guard with the secret scan: it invokes scripts/hooks/pre-commit-secret-scan.sh resolved via git rev-parse --show-toplevel and propagates its exit code; main guard preserved. install-hooks.sh switched to approach (a): drops the dead $GIT_DIR/hooks shim/backup logic and instead sets core.hooksPath=.githooks idempotently (git config) with confirmation output; justfile install-hooks comment updated. Added 3 regression tests in test-secret-scan-hook.sh that stand up a throwaway repo wired like ProjectHana (copy .githooks/ + scan script, set core.hooksPath=.githooks) and drive real git commit: planted token REJECTED, clean non-main commit succeeds, direct-to-main blocked. lint-sh clean (incl. shellcheck -s sh on .githooks/pre-commit); test-bot-scripts 16/16 green. No real token anywhere (fake tokens runtime-constructed + ghp_FAKE sentinel). No Swift/Python touched -> just install skipped.
