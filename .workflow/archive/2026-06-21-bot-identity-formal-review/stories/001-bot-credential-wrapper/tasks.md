## Tasks
- [x] 001: Create `scripts/gh-review-bot.sh` (committed, `chmod +x`): `#!/usr/bin/env bash`, `set -euo pipefail`, never enable `-x`/xtrace. Read the bot token from the macOS Keychain service `hana-review-bot` via `security find-generic-password -s hana-review-bot -w` into a local variable, export it ONLY into the child via `GH_TOKEN=... exec "$@"`. Never `echo`/`printf`/redirect the token to stdout/stderr/any file. Pass all args through unchanged.
- [x] 002: Add the absent-Keychain-item failure path to `scripts/gh-review-bot.sh`: detect a non-zero/empty result from `security find-generic-password`, then exit non-zero (do NOT exec the underlying command) with a clear, actionable stderr message that names the service `hana-review-bot` and points at the setup docs (`docs/bot-credentials.md`).
- [x] 003: Add a guard at the top of `scripts/gh-review-bot.sh` that errors when invoked with no arguments (nothing to exec), with a usage message.
- [x] 004: Create the secret-scanning pre-commit hook `scripts/hooks/pre-commit-secret-scan.sh` (committed, `chmod +x`): scan staged content (`git diff --cached`) for `github_pat_…` and `ghp_…` token patterns; on a match, exit non-zero with a clear message identifying the offending pattern/file; on no match, exit 0 silently. Must not match ordinary content.
- [x] 005: Create the install/wiring step `scripts/install-hooks.sh` (committed, `chmod +x`) that registers `scripts/hooks/pre-commit-secret-scan.sh` as the repo's `.git/hooks/pre-commit` (symlink or thin shim that calls the committed script), idempotently, with a success message.
- [x] 006: Add a `just install-hooks` recipe that runs `scripts/install-hooks.sh`, so fresh clones can activate the hook with one command.
- [x] 007: Create a committed bash test harness `scripts/test-gh-review-bot.sh` (no `bats` dependency) covering, all WITHOUT the real token: (a) absent-Keychain-item error path exits non-zero with the actionable message and does NOT exec the target; (b) no-token-leak — token value never appears in stdout/stderr/any written file; (c) arg-passthrough via a `gh`/`security` stub on `PATH` asserting the underlying command receives the exact args with `GH_TOKEN` set. Use stubbed `security` (fake token) + stub target command.
- [x] 008: Create a committed bash test harness `scripts/test-secret-scan-hook.sh` covering: (a) the hook rejects a PLANTED fake token `ghp_FAKE0000000000000000000000000000000000` (and a `github_pat_…` sample) in staged content with non-zero exit; (b) the hook does NOT trigger on ordinary staged content (exit 0). Use an isolated temp git repo / staged fixtures so it never touches the real index.
- [x] 009: Add a `just test-bot-scripts` recipe (and wire into `just lint`/CI as appropriate) that runs `scripts/test-gh-review-bot.sh` and `scripts/test-secret-scan-hook.sh`.
- [x] 010: Run `just lint-sh` (shellcheck) over all new scripts and fix every finding; ensure the new `.sh` files are tracked so they are linted.
- [x] 011: Create `docs/bot-credentials.md` documenting: the 3 human prerequisites (create the `Hanahuac-Bot` fine-grained PAT, store it in Keychain under service `hana-review-bot`, run `just install-hooks`), the exact `security add-generic-password -s hana-review-bot -a <acct> -w` setup command, usage of `scripts/gh-review-bot.sh`, the pre-commit secret-scan hook, and the token-rotation procedure. Add a short pointer from `README.md` (Documentation section).
