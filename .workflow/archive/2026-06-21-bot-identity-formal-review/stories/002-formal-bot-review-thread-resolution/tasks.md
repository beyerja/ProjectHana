## Tasks

- [x] 001: In `independent-review.md`, replace the "GitHub self-review constraint — verdict via STATUS, never a formal review state" section. The old self-review block forbids `gh pr review --approve/--request-changes` because the PR-opener cannot self-approve. Rewrite it to: (a) keep STATUS as the authoritative loop signal (additive), and (b) introduce the bot identity — formal review states are now submitted under `Hanahuac-Bot` *through the wrapper* `scripts/gh-review-bot.sh`, which is a different account and so is NOT blocked. Delete the "out of scope / Do NOT build it" note that previously deferred this. (AC1, AC2)

- [x] 002: In `independent-review.md`, document the formal-review SUBMISSION mechanism. After `/code-review --comment` produces the verdict, the agent submits a FORMAL GitHub review through the bot wrapper: `APPROVE` when clean, `REQUEST_CHANGES` otherwise. Specify the exact invocation shape — `scripts/gh-review-bot.sh gh pr review <number> --approve` and `scripts/gh-review-bot.sh gh pr review <number> --request-changes --body-file <file>` — and state explicitly that the review *logic* is still owned by `/code-review` (only the submission changes). Reuse the existing rule that bodies go via `--body-file` (never `--body "$(…)"`, never heredoc). (AC1, AC7)

- [x] 003: In `independent-review.md`, specify thread resolution via the `resolveReviewThread` GraphQL mutation through the bot wrapper. Give the exact `scripts/gh-review-bot.sh gh api graphql` invocation shape: the `query { ... pullRequest { reviewThreads } }` to enumerate unresolved thread node ids, then `mutation { resolveReviewThread(input: {threadId: $threadId}) { thread { isResolved } } }` invoked as `scripts/gh-review-bot.sh gh api graphql -f query='…' -F threadId=<id>`. State that a reply comment alone does NOT resolve a thread — the mutation is required — and that thread ids are GraphQL node ids (not REST databaseIds). (AC4, AC6, AC7)

- [x] 004: In `independent-review.md`, document graceful degradation. Because the wrapper exits non-zero and does NOT run the underlying command when the Keychain item `hana-review-bot` is absent (fail-closed, per `scripts/gh-review-bot.sh`), the agent must detect that non-zero exit and fall back to: a `COMMENT`-type review (`gh pr review <number> --comment --body-file <file>`, runnable as the PR-opener) plus STATUS, and explicitly record in the summary comment / log that the formal review state and thread resolution were SKIPPED. State that this fallback is the documented DEFAULT until the bot token is provisioned, and that STATUS remains the authoritative loop signal either way. (AC5, AC2)

- [x] 005: In `independent-review.md`, reinforce the token-safety invariant: the agent NEVER prints or reads the bot token directly; every bot-auth call (formal review, `resolveReviewThread`, any bot `gh api`) goes through `scripts/gh-review-bot.sh`. Cross-reference the wrapper's own credential-safety invariants. (AC6)

- [x] 006: In `independent-review.md`, update the "GitHub self-review constraint" removal's downstream references — the `## Steps` list (step 5 now also submits the formal review or the COMMENT fallback) and the "Feedback-loop contract" section (which currently says `resolveReviewThread` is "out of scope here"). Make the contract describe the now-in-scope thread resolution and which actor performs it. Keep STATUS emission (step 7) unchanged. (AC1, AC2, AC4)

- [x] 007: In `story-workflow.md` step 5 (independent review loop), update the two parenthetical "out of scope here" notes about `resolveReviewThread` to reflect that thread resolution is now performed (via the bot wrapper) after the implement agent addresses comments. Specify which agent resolves threads (the implement agent acknowledging + the bot resolving, or the reviewer on re-spawn) and keep the loop keyed off STATUS, capped at 3 rounds. Do not change the STATUS-branching logic. (AC2, AC4)

- [x] 008: Sanity pass — re-read both edited agent files end-to-end for internal consistency: no lingering "NEVER run `gh pr review --approve`" contradictions, no leftover "out of scope" notes for thread resolution, all command shapes obey CLAUDE.md (no `cd &&`, no heredocs, bodies via `--body-file`/`-F`), and every spec AC (1-7) is traceable to instruction text. (AC1-AC7)
