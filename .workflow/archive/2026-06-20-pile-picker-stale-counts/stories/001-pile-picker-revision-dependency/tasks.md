## Tasks
- [x] 001: In `Hanahuac/Views/Home/PilePickerView.swift`, add `_ = cardStore.revision` at the top of `body` (value discarded), refactoring `body` to `return` the existing `ScrollView`, mirroring the `HomeView.body` #114 pattern with an equivalent explanatory comment. The `newCount`/`pendingCount` computed accessors read no `@Observable` stored property, so this revision read is what ties the view's invalidation to CardStore writes.
- [x] 002: Add a regression test to `HanahuacTests/CardStoreTests.swift` (under the existing "Revision signal (Observation regression guard)" section) that mutates a card into a state which removes it from the new/pending piles, routes the save through `store.persistCardChanges()`, and asserts both that `store.revision` bumped and that `newCards(for:)`/`dueCards(for:)` now reflect the changed count — proving the data path the pile picker's revision dependency relies on. Follow existing CardStoreTests conventions (setup via `store`, `geoData`, fresh-store re-read where helpful).
- [x] 003: Run `just lint` and `just test` from the worktree; fix any lint/test failures introduced by tasks 001–002.
