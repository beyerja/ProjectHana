## Tasks
- [x] 001: Cut the story branch `story/agent-ui-driver/003-just-recipes-and-docs` off `feat/agent-ui-driver` (export HANA_FEATURE_SLUG="agent-ui-driver"); confirm story-001 driver files (HanahuacUITests/UIDriverTests.swift, UIActionScript.swift, UIWalkthroughRecorder.swift) are present on the branch.
- [x] 002: Tree-hygiene — delete root debris files `verify-script.json` and `verify001.xctestplan` (use git rm if tracked; plain rm if untracked — both are currently untracked).
- [x] 003: Tree-hygiene — remove the stray story-001 verify run-output dirs `.workflow/ui-walkthrough/verify-empty/` and `.workflow/ui-walkthrough/verify-script/` (untracked); leave the parent `.workflow/ui-walkthrough/` ready for the canonical `scripts/` + `README.md` structure.
- [x] 004: Create the canonical action-scripts dir and committed example `.workflow/ui-walkthrough/scripts/smoke.json` with exactly `[ {"action":"wait","seconds":1}, {"action":"dumpTree"} ]` (matches the UIActionStep schema in HanahuacUITests/UIActionScript.swift).
- [x] 005: Add `scripts/ui-walkthrough.sh` helper — shebang + `set -euo pipefail`, quoted vars, shellcheck-clean (following scripts/verify-odr-packs.sh & scripts/backup-progress.sh conventions). Resolves repo root, resolves/creates the run name, exports HANA_UI_SCRIPT_PATH + HANA_REPO_ROOT + HANA_UI_RUN, invokes the xcodebuild test (passed in or run directly with -only-testing:HanahuacUITests/UIDriverTests/testRunUIScript and the sim destination + per-worktree DerivedData), and echoes the final artifact dir `.workflow/ui-walkthrough/<run>/`.
- [x] 006: Add the `ui-walkthrough script run=""` recipe to the justfile (place near the sim recipes ~lines 195-234). Style-match existing recipes: `{{sim}}`/`{{sim_dd}}`/`wt`-aware DerivedData, tools via direnv/flake, allowlistable body (no `cd &&`, no heredocs, no committed `$(…)`, no poll loops) — delegate the glue to `bash scripts/ui-walkthrough.sh`. Defaults `script` to the committed smoke.json and `run` to empty (recorder timestamps). Prints the artifact dir on success.
- [x] 007: Author `.workflow/ui-walkthrough/README.md` documenting: the action-script JSON schema + every supported action (tap/typeText/mapTap/swipe/scroll/wait/dumpTree/screenshot) and optional fields (label/identifier/text/x/y/direction/seconds) per UIActionScript.swift; the env-var contract (HANA_UI_SCRIPT_PATH preferred / HANA_UI_SCRIPT inline, HANA_REPO_ROOT, HANA_UI_RUN); canonical locations (scripts at `.workflow/ui-walkthrough/scripts/<name>.json`, artifacts at `.workflow/ui-walkthrough/<run>/NNN-step.png|json`); the write-script -> run -> read-artifacts loop; and the performance reality ("each run is a compiled xcodebuild test cycle ~tens of seconds, NOT live frame-by-frame").
- [x] 008: Update `.gitignore` to ignore ui-walkthrough run-output dirs while keeping the committed `.workflow/ui-walkthrough/scripts/` and `.workflow/ui-walkthrough/README.md` tracked (e.g. ignore `.workflow/ui-walkthrough/*` then negate `!.../scripts/`, `!.../scripts/**`, `!.../README.md`). Verify with `git status`/`git check-ignore` that smoke run dirs are ignored and the example script + README remain tracked.
- [x] 009: Prove wiring — boot the iPhone 17 / iOS 26.5 sim and run `just ui-walkthrough` (smoke.json default) against it. Run must succeed and produce at least `000-step.png` + `000-step.json` under a run dir; verify via Glob/Read. Do NOT commit the smoke run output.
- [x] 010: Run `just lint` (swift/py/sh/nix/yaml/l10n) and confirm green — especially `just lint-sh` over the new scripts/ui-walkthrough.sh.
- [x] 011: Stage ONLY the intended paths (justfile, scripts/ui-walkthrough.sh, .workflow/ui-walkthrough/README.md, .workflow/ui-walkthrough/scripts/smoke.json, .gitignore change, and the two root debris deletions) — never `git add -A`/`git add .`. Confirm via `git status` that no run-output PNGs/JSON are staged.
