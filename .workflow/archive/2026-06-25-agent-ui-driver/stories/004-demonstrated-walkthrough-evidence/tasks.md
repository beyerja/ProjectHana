## Tasks
- [x] 001: Discover the live Home tree — run `just ui-walkthrough script=".workflow/ui-walkthrough/scripts/smoke.json" run="discover"` (with HANA_FEATURE_SLUG="agent-ui-driver"), then Read the emitted 000-step.json / 000-step.png to confirm reachable quiz-mode buttons (home.mode.<id>) and whether tapping a mode lands directly in the quiz or on the PilePicker (NavigationLink, no identifier). Record findings in log.md.
- [x] 002: Author the demo action script `.workflow/ui-walkthrough/scripts/full-walkthrough.json` driving Home -> open a quiz mode -> interact -> Settings. Pick the mode from 001 (multipleChoice via home.mode.multiple_choice is the safest non-map interaction path; mapQuiz via home.mode.map_tap enables mapTap). The script must COLLECTIVELY exercise every supported action: tap (by identifier and/or label), typeText, mapTap (normalized x/y), swipe or scroll, wait, dumpTree, screenshot. Use waits after each navigation tap. Validate it is well-formed JSON (array of step objects per README schema).
- [x] 003: Run the demo script to a fixed run dir: `just ui-walkthrough script=".workflow/ui-walkthrough/scripts/full-walkthrough.json" run="demo"` (export HANA_FEATURE_SLUG="agent-ui-driver"). Confirm the recipe completed and that `.workflow/ui-walkthrough/demo/` contains paired NNN-step.png + NNN-step.json for every step including 000.
- [x] 004: Verify the evidence shows distinct real screens — Read the key PNGs (Home, the quiz mode, Settings) with the Read tool. If any step lands on the wrong screen (missed tap, PilePicker intercept, premature screenshot), fix targeting/waits in full-walkthrough.json and re-run task 003 to the same `demo` dir; repeat until Home, a quiz mode, and Settings are all visibly captured.
- [x] 005: Make the `demo` run dir committable — add a narrow un-ignore rule to `.gitignore` for `.workflow/ui-walkthrough/demo/` (current rules ignore `.workflow/ui-walkthrough/*` except scripts/ and README.md). Stage the dir, then VERIFY tracked-not-ignored via `git status --porcelain` and `git ls-files .workflow/ui-walkthrough/demo/`. Use `git add -f` only if needed alongside the un-ignore rule.
- [x] 006: Write the step index — add `.workflow/ui-walkthrough/demo/README.md` (or append a section to the walkthrough README) mapping each step number -> action performed -> screen reached, so a reviewer can follow Home -> quiz mode -> Settings. Ensure it is tracked (covered by the 005 un-ignore rule or add -f).
- [x] 007: Final consistency check — confirm full-walkthrough.json (script), the demo PNG/JSON artifacts, the step index, and the .gitignore rule are all staged and tracked; re-run `git status --porcelain` + `git ls-files` to prove nothing intended-to-commit is still ignored.
