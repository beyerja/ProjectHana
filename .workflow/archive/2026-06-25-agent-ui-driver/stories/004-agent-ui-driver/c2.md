**Non-blocking (scope nit):** the feature spec lists *"Mac Catalyst driving is explicitly out of scope"* and *"iOS Simulator only"*, yet the new UI-test target sets `SUPPORTS_MACCATALYST: YES` (and a `MACOSX_DEPLOYMENT_TARGET`). This is harmless for the iOS-Simulator driver path that the recipe actually uses, but it is dead/contradictory config relative to the stated scope. Consider dropping the Catalyst settings unless a follow-up intends to drive Catalyst.
