## Tasks
- [x] 001: Add a `HanahuacUITests` target to `project.yml` (type `bundle.ui-testing`, platform iOS, `deploymentTarget: "17.0"`), matching the `HanahuacTests` shape: `sources: - path: HanahuacUITests`, a `settings.base` block with `PRODUCT_BUNDLE_IDENTIFIER: com.hanahuac.app.uitests`, `GENERATE_INFOPLIST_FILE: YES`, `SUPPORTS_MACCATALYST: YES`, `MACOSX_DEPLOYMENT_TARGET: "14.0"`, `SWIFT_VERSION: "5.10"`, and `dependencies: - target: Hanahuac`. Add `HanahuacUITests` to the `Hanahuac` scheme's `test.targets` list (alongside `HanahuacTests`). Do NOT edit pbxproj by hand.
- [x] 002: Define the action-script model + decoder in a new file `HanahuacUITests/UIActionScript.swift`. A `Codable` step model decoding the supported action set: `tap` (with `label` OR `identifier`), `typeText` (`text`, plus target `label`/`identifier`), `mapTap` (normalized `x`,`y` in 0...1), `swipe`/`scroll` (direction + optional target), `wait` (seconds), `dumpTree`, `screenshot`. Provide a loader that reads JSON from `HANA_UI_SCRIPT_PATH` (file path) or, failing that, `HANA_UI_SCRIPT` (inline JSON), returning an EMPTY step list when both are missing/blank or the JSON is empty (no crash, no throw to the test body).
- [x] 003: Implement the per-step artifact emitter in `HanahuacUITests/UIWalkthroughRecorder.swift`: resolves a run dir under `.workflow/ui-walkthrough/<run>/` (derive `<run>` from a timestamp or `HANA_UI_RUN` env override; create the directory), and for a given zero-padded step index `NNN` writes `NNN-step.png` (a captured `XCUIScreenshot`) and `NNN-step.json` (recursive accessibility-element dump capturing `type`, `label`, `identifier`, `value`, `frame` for each element). Resolve the repo root for the `.workflow/` path via an env var (e.g. `HANA_REPO_ROOT`) with a sensible fallback, since the UI test process runs in the simulator sandbox.
- [x] 004: Implement the single generic driver test in `HanahuacUITests/UIDriverTests.swift` (one `XCTestCase` with one test method): launch the `Hanahuac` app via `XCUIApplication`, ALWAYS emit an initial dump+screenshot (step 000) before processing the script, then execute each loaded action in order using element queries that match by accessibility LABEL first (fall back to identifier when provided) — covering `tap`, `typeText`, `mapTap` (normalized coordinate via `XCUICoordinate`), `swipe`/`scroll`, `wait`, `dumpTree`, `screenshot` — and after EVERY step write `NNN-step.png` + `NNN-step.json` via the recorder. An empty/missing script must still launch the app and produce the initial artifacts without failing the test.
- [x] 005: Regenerate `Hanahuac.xcodeproj` by running `just generate` (xcodegen via direnv). Confirm the `HanahuacUITests` target and its scheme test-action membership appear in the regenerated project. Never hand-edit the pbxproj.
- [x] 006: Verify the `HanahuacUITests` target BUILDS for the iOS Simulator. Run `xcodebuild build-for-testing -project Hanahuac.xcodeproj -scheme Hanahuac -destination 'platform=iOS Simulator,name=iPhone 17'` (iOS 26.5) using the per-worktree derivedDataPath convention from the justfile, and confirm it compiles green. Fix any compile errors in the new sources. (Running the test itself is exercised in later stories; this task only asserts the build.)
