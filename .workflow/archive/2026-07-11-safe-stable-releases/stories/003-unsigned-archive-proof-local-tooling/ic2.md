Non-blocking (note for stories 004/005): this section is dated 2026-07-05, but no v1.0.0 tag exists yet. `check-tag-version` validates the version string, not the date, so if the actual tag lands on a later date this heading silently goes stale. The section itself is correct and required here (`check-changelog` derives 1.0.0 from project.yml and `[Unreleased]` never satisfies it, per the documented release procedure) — just make sure the release runbook (story 005) includes refreshing this date at tag time if it drifts.
