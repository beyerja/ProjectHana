## Tasks

- [x] 001: Add `case ca` to `AppLocale` in `Hanahuac/L10n/AppLocale.swift`, placed in catalog order (immediately after `esES`, before `ko`). Do NOT touch `matching(_:)` Spanish handling — `ca` auto-detects via the existing catalog `code`-lookup path (code `ca` == rawValue) and must not perturb the `es == .esMX` mapping. Confirm the doc comment about resolution order still reads correctly.
- [x] 002: Add the `ca` `LanguageDescriptor` to `Hanahuac/L10n/LanguageCatalog.swift` in the same position as the enum case: displayName "Català", fallbackChain `[.ca, .esES, .en]`, availability `.downloadablePack`, odrTags default to `[lang-ca]`. Update the catalog header comment's parenthetical language list to include `ca`.
- [x] 003: Create `Hanahuac/ca.lproj/Localizable.strings` — author real, professional-quality Catalan for the ~85 UI keys, using `en.lproj` as the authoritative key set. Best-effort: keys with a genuine gap may be omitted so they fall back through `ca → es-ES → en`. Deliberately leave at least one key untranslated that IS present in `es-ES.lproj` so the fallback-chain test (task 008) can assert resolution to the es-ES value before en.
- [x] 004: Add Catalan geo columns to the bundled source data — `name_ca` + `capital_ca` in `Hanahuac/Resources/countries.json`, and `name_ca` in `rivers.json`, `mountains.json`, `seas.json`. Author real professional Catalan to the extent feasible (best-effort coverage permitted; omit a genuine gap rather than guess).
- [x] 005: Extend `scripts/generate-geo-packs.py` — add `"ca"` to `PACK_LANGUAGES` and `"ca": "ca"` to `SUFFIX_BY_CODE`. Update the module docstring's language enumeration to include `ca`.
- [x] 006: Generate `Hanahuac/Resources/ca-geo.json` via `just geo-packs`, then confirm `just geo-packs-check` passes (committed pack matches generated output).
- [x] 007: Wire ODR packaging in `project.yml` — add `ca.lproj` and `Resources/ca-geo.json` to the `excludes` list, and add the two resource entries tagged `[lang-ca]` (folder reference for `ca.lproj`, file for `ca-geo.json`), mirroring the `es-ES` block. Update the surrounding comments' language lists. Regenerate the Xcode project via `just generate`.
- [x] 008: Add the fallback-chain test asserting a deliberately un-translated `ca` UI key resolves to the `es-ES` value BEFORE `en` (chain order routes through Spanish). Follow the `L10nBundleResolutionTests` pattern (PackAbsentProvider or ODRTestSupport as appropriate); also assert `L10n.bundleCandidates(for: .ca) == ["ca", "es-ES", "en"]` and `AppLocale.ca.fallsBackThroughSpanish` semantics for the es-ES route.
- [x] 009: Extend catalog/enum invariant tests for `ca` — in `LanguageCatalogTests` bump the count assertion (7 → 8), add `ca` to the displayName/fallbackChain/availability/ODR-tag assertions and the `.downloadablePack` locale list; in `AppLocaleTests` bump `testAllCasesCount` (7 → 8), assert `AppLocale.ca.displayName == "Català"` and catalog ordering (ca immediately after es-ES), and add a regression assertion that `AppLocale.matching(Locale(identifier: "es_ES")) == .esMX` and `matching(Locale(identifier: "ca")) == .ca` (es-* mapping unperturbed).
- [x] 010: Add a per-language progress isolation assertion for `.ca` in `PerLanguageProgressTests` (e.g. `assertProgressIsolated(.ca, .esES)`), reusing the existing helper without changing its semantics.
- [x] 011: Update the remaining `AppLocale.allCases`-driven / hardcoded-locale-list tests for the new case — `BundledLanguagePackProviderTests` and `ODRLanguagePackProviderTests` (add `.ca` to any hardcoded downloadable-locale lists; the generic `allCases` loops cover it automatically). Confirm no test still hardcodes a count of 7 anywhere.
- [x] 012: Run the full local verification gate and fix any fallout — `just lint`, `just test`, `just geo-packs-check`, `just verify-odr-packs` (ca NOT bundled, tagged `[lang-ca]`), and an iOS/Catalyst build (`just build-mac`).
