## Tasks

- [x] 001: Add `case pl` to `AppLocale` in `Hanahuac/L10n/AppLocale.swift`, placed after `case it` and before `case ko` to match `allCases` order. Update the `matching(_:)` doc comment to mention `pl` resolves via the catalog `code == rawValue` lookup; confirm no `es-*`/`nah` mapping is perturbed (no explicit arm needed — the catalog lookup handles it).
- [x] 002: Add the `pl` descriptor to `LanguageCatalog.all` in `Hanahuac/L10n/LanguageCatalog.swift`, inserted after `it` and before `ko`. Use displayName "Polski", fallbackChain `[.pl, .en]`, availability `.downloadablePack`. Mirror the Italian comment noting `pl` is COMPLETE-content (routes straight to `en` as a never-hit safety net, NOT through es-MX/es-ES). Update the header comment's ordered language list to include `pl`.
- [x] 003: Audit existing extensions on the geo model types — grep for `extension Country`, `extension River`, `extension MountainRange`, `extension Sea` across the project and note any presentation/raw-field helpers that must be extended (not re-declared) when adding the `pl` switch arm and `namePl`/`capitalPl` fields.
- [x] 004: Add `namePl` (and `capitalPl` for `Country`) optional fields to the geo models in `Hanahuac/Models/Country.swift`, `River.swift`, `MountainRange.swift`, `Sea.swift`. Place each `pl` field after the `it` field, with `= nil` defaults in the memberwise init to preserve source compatibility. Decoding relies on `.convertFromSnakeCase` (name_pl ↔ namePl), so no CodingKeys change is needed.
- [x] 005: Audit and update all existing tests that construct `Country`/`River`/`MountainRange`/`Sea` for the new `namePl`/`capitalPl` fields — the `= nil` defaults should keep call sites compiling, but verify no fixture/test asserts an exhaustive field list or memberwise-arg count that the new fields break.
- [x] 006: Add `case .pl: namePl` (and `case .pl: capitalPl` for `Country`) switch arms to all four `rawName(for:)`/`rawCapital(for:)` extensions in `Hanahuac/L10n/Packs/GeoModel+PackData.swift`, placed after the `.it` arm in each switch, restoring exhaustiveness after the new enum case.
- [x] 007: Create `Hanahuac/pl.lproj/Localizable.strings` with COMPLETE professional Polish for ALL 156 canonical UI keys from `Hanahuac/en.lproj/Localizable.strings`. Use correct Polish diacritics (ą, ć, ę, ł, ń, ó, ś, ź, ż), zero missing keys, zero English/other-language leakage. Mirror the file structure/comment layout of `Hanahuac/it.lproj/Localizable.strings`.
- [x] 008: Add `name_pl` + `capital_pl` to EVERY country (197 entries) in `Hanahuac/Resources/countries.json`, professional Polish endonyms/exonyms with correct diacritics. Complete coverage — no country left without a `pl` value.
- [x] 009: Add `name_pl` to EVERY entity in `Hanahuac/Resources/rivers.json` (32), `mountains.json` (23), and `seas.json` (20), professional Polish with correct diacritics. Complete coverage for all three files.
- [x] 010: Wire `pl` into `scripts/generate-geo-packs.py` — add `"pl"` to `PACK_LANGUAGES` (after `"it"`) and `"pl": "pl"` to `SUFFIX_BY_CODE`; update the module docstring's language list to include `pl`.
- [x] 011: Generate `Hanahuac/Resources/pl-geo.json` by running `just geo-packs`, then verify with `just geo-packs-check` that the committed pack is up to date.
- [x] 012: Wire `pl` ODR resources into `project.yml` — add `Resources/pl-geo.json` and `pl.lproj` to the `excludes` list (after the `it` entries), and add the two tagged resource entries (`Hanahuac/pl.lproj` as `type: folder` and `Hanahuac/Resources/pl-geo.json`, both `buildPhase: resources`, `resourceTags: [lang-pl]`) after the `it` block. Update the explanatory comments' language lists to include `pl`. Regenerate the pbxproj via `just generate`.
- [x] 013: Add `pl` to the code lists in `scripts/verify-odr-packs.sh` (the `LANG_CODES` array and the header comment's `lang-<code>` list) and in `scripts/verify-base-only-bundle.sh` (both `for code in …` loops and the header comment).
- [x] 014: Add `"pl"` to `FULL_LOCALES` in `scripts/check-l10n-completeness.py` (alongside fr/de/es-ES/ko/it) and update the module docstring's full-locale list. Do NOT add blanket allowances — `pl` is enforced complete; add a scoped `("pl", <key>)` allowance only for any key whose value is legitimately identical to English (e.g. format-string-only or shared proper noun) and document why.
- [x] 015: Update `HanahuacTests/LanguageCatalogTests.swift` — rename `testCatalogContainsExactlyElevenLanguages` to `testCatalogContainsExactlyTwelveLanguages` and change the asserted count from 11 to 12; add `XCTAssertEqual(LanguageCatalog.descriptor(for: .pl).displayName, "Polski")`; add `.pl` to the downloadablePack iteration list and assert `fallbackChain == [.pl, .en]` plus `fallsBackThroughSpanish == false`; add `XCTAssertEqual(AppLocale.pl.odrTags, ["lang-pl"])`.
- [x] 016: Add `pl` coverage to the remaining tests — `HanahuacTests/AppLocaleTests.swift` (native display name / `matching(_:)` resolution for a `pl` locale), `HanahuacTests/L10nBundleResolutionTests.swift` (`bundleCandidates(for: .pl) == ["pl", "en"]` and lproj bundle resolution), and `HanahuacTests/PerLanguageProgressTests.swift` (`assertProgressIsolated(.pl, .en)`).
- [x] 017: Run the full gate locally — `just lint`, `just test`, `just geo-packs-check`, `just verify-odr-packs`, and an iOS/Catalyst build — and fix any failures so all pass before opening the PR.
