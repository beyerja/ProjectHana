## Tasks

<!--
Confirmed against the current codebase (re-validation 2026-06-25):
- Canonical UI key count: 156 (en.lproj == it.lproj == pl.lproj == 156). The spec body's "~85" is
  WRONG; nl.lproj MUST contain all 156 keys with ZERO missing vs en.lproj.
- Catalog language count: currently 12 (LanguageCatalog.all.count == 12; AppLocale.allCases.count == 12);
  nl is NOT yet present. Target after this story: 13.
- Geo entity counts: countries.json = 197, rivers.json = 32, mountains.json = 23, seas.json = 20.
  Dutch is COMPLETE-content → every entity needs name_nl (and capital_nl for countries); zero gaps.
- nl ordering: AppLocale + LanguageCatalog place nl AFTER pl, BEFORE ko. PackData / scripts place nl
  AFTER pl. fallbackChain[nl] = [.nl, .en] (straight to en; NO Spanish hop). availability .downloadablePack,
  odrTags [lang-nl].
- es-MX device-locale default selection is UNCHANGED: matching(_:) resolves nl via the generic catalog
  code lookup (code "nl" == rawValue); the es-* -> esMX mapping must remain untouched.
-->

- [x] 001: Add `case nl` to `AppLocale` in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/L10n/AppLocale.swift`, immediately after `case pl` (line 13) and before `case ko`, matching the `AppLocale.allCases` order. Add `nl` to the code-lookup list in the `matching(_:)` doc comment (the enumerated list `en, fr, de, ca, eu, yua, it, pl, ko`). Confirm NO special-casing is added: `nl` resolves via the existing generic `LanguageCatalog.all.first(where: code == language)` lookup (code `nl` == rawValue), and the `es == "es"` → `.esMX` mapping above MUST stay untouched.

- [x] 002: Add the `nl` `LanguageDescriptor` to `LanguageCatalog.all` in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/L10n/LanguageCatalog.swift`, placed after the `pl` descriptor (ends line 82) and before `ko`. displayName "Nederlands", `fallbackChain: [.nl, .en]`, `availability: .downloadablePack`, `odrTags` default `[lang-nl]` (derived from availability+code). Precede it with the same COMPLETE-content comment used for Italian/Polish (routes straight to en, NOT through es-MX/es-ES). Update the ordering comment on lines 10-12 (`en, fr, de, es-MX, es-ES, ca, eu, yua, it, pl, ko, nah`) to insert `nl` after `pl`.

- [x] 003: Add optional `nameNl` (and `capitalNl` for `Country`) fields to the four geo model structs: `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/Models/Country.swift`, `River.swift`, `MountainRange.swift`, `Sea.swift`. In each: declare `let nameNl: String?` immediately after `let namePl` (and `let capitalNl: String?` after `let capitalPl` in `Country`), add the `nameNl: String? = nil` (and `capitalNl: String? = nil`) defaulted init parameter in the same position, and the `self.nameNl = nameNl` (and `self.capitalNl = capitalNl`) assignment. Match the existing pl pattern exactly so all call sites stay source-compatible.

- [x] 004: Audit `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/HanahuacTests/` for any code that constructs `Country`/`River`/`MountainRange`/`Sea` (grep the initializers). The new `nameNl`/`capitalNl` params are optional with `nil` defaults so existing call sites should remain source-compatible; confirm none pass arguments positionally in a way that the inserted parameter would shift, and fix any that do.

- [x] 005: Add `.nl` switch arms to all four `rawName(for:)` extensions (and `Country.rawCapital(for:)`) in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/L10n/Packs/GeoModel+PackData.swift`: insert `case .nl: nameNl` after the `case .pl: namePl` arm in each of the four `rawName(for:)` switches, and `case .nl: capitalNl` after `case .pl: capitalPl` in `Country.rawCapital(for:)`. Adding the enum case will make these switches non-exhaustive until done, so this task is required for compilation.

- [x] 006: Create the durable, idempotent seed script `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/scripts/seed-nl-geo.py`, modeled on `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/scripts/seed-pl-geo.py`. Define id-keyed maps `COUNTRY_NAME_NL` (197 countries), `COUNTRY_CAPITAL_NL` (197 capitals), `RIVER_NAME_NL` (32), `MOUNTAIN_NAME_NL` (23), `SEA_NAME_NL` (20) covering EVERY entity with professional Dutch. Use established Dutch exonyms (Duitsland, Spanje, Italië, Frankrijk, China, Rusland, Moskou, Londen, Peking, Middellandse Zee, Alpen, Pyreneeën, Donau, Rijn) and keep the local/native name where Dutch has no distinct exonym. Insert `name_nl` immediately after `name_pl` (and `capital_nl` after `capital_pl`) in each JSON entry, preserving key order. Idempotent: re-running rewrites only the nl columns.

- [x] 007: Run `python3 /Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/scripts/seed-nl-geo.py` to populate `name_nl` (and `capital_nl`) for EVERY entity in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/Resources/countries.json` (197), `rivers.json` (32), `mountains.json` (23), `seas.json` (20). Verify zero missing nl values (every entity that has an en value must have a non-empty nl value).

- [x] 008: Create `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/nl.lproj/Localizable.strings` — NEW file with COMPLETE professional Dutch for ALL 156 canonical UI keys, mirroring the en.lproj key set EXACTLY (zero missing keys). Use `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/en.lproj/Localizable.strings` as the key template and `pl.lproj` as the structural precedent.

- [x] 009: Add `"nl"` to the `FULL_LOCALES` tuple in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/scripts/check-l10n-completeness.py` (currently `("de", "fr", "es-ES", "it", "pl", "ko")`) so nl is enforced to the full 156-key canonical set, and update the docstring (b) line that enumerates the fully-translated locales. Add any genuinely-identical-to-English nl keys (e.g. continent proper nouns spelled the same in Dutch such as "Afrika"/"Azië" only if byte-identical, the "iCloud" brand `settings.section.icloud`, the "%d / 3" format `learn_map.streak`) to `IDENTICAL_VALUE_ALLOWLIST` as `("nl", <key>)` pairs — only for values byte-identical to English; keep the list minimal and justified.

- [x] 010: Add `"nl"` to `PACK_LANGUAGES` and `"nl": "nl"` to `SUFFIX_BY_CODE` in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/scripts/generate-geo-packs.py`, both placed after the `pl` entry. Update the module docstring's language list (line 4) to include `nl`.

- [x] 011: Generate `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac/Resources/nl-geo.json` via `HANA_FEATURE_SLUG=additional-language-support just geo-packs`. Verify it is well-formed `GeoNamePackData` (`version: 1`, `code: "nl"`, non-empty `entries`) and that `HANA_FEATURE_SLUG=additional-language-support just geo-packs-check` passes (nl pack up to date).

- [x] 012: Wire ODR in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/project.yml`: in the `Hanahuac` target `excludes`, add `- "Resources/nl-geo.json"` after the `pl-geo.json` line and `- "nl.lproj"` after the `pl.lproj` line. In the resources list, add the two entries after the `pl` block — `- path: Hanahuac/nl.lproj` (type: folder, `resourceTags: [lang-nl]`) and `- path: Hanahuac/Resources/nl-geo.json` (`resourceTags: [lang-nl]`), mirroring the `pl` block exactly. Update the descriptive comments on lines 26 and 60 that enumerate the non-base languages to include `nl`.

- [x] 013: Regenerate the Xcode project from project.yml via `HANA_FEATURE_SLUG=additional-language-support just generate` so `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/Hanahuac.xcodeproj/project.pbxproj` includes the `lang-nl` knownAssetTag and the tagged `nl.lproj` + `nl-geo.json` resources.

- [x] 014: Add `nl` to the verification shell scripts: in `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/scripts/verify-odr-packs.sh` add `nl` to `LANG_CODES=(fr de es-ES ca eu yua it pl ko nah)` (after `pl`) and to the tag-contract comment on line 8. In `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/scripts/verify-base-only-bundle.sh` add `nl` (after `pl`) to BOTH `for code in fr de es-ES it pl ko nah` loops (lines ~48 and ~56) and to the `knownAssetTags` comment (line ~10). Run `HANA_FEATURE_SLUG=additional-language-support just verify-odr-packs` to confirm green.

- [x] 015: Update `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/HanahuacTests/LanguageCatalogTests.swift`: rename `testCatalogContainsExactlyTwelveLanguages` → `testCatalogContainsExactlyThirteenLanguages` and change `XCTAssertEqual(LanguageCatalog.all.count, 12)` to `13`; add `XCTAssertEqual(LanguageCatalog.descriptor(for: .nl).displayName, "Nederlands")` in `testDisplayNamesMatchExpectedNativeStrings`; add `XCTAssertEqual(AppLocale.nl.odrTags, ["lang-nl"])` in `testDownloadableLanguagesYieldExpectedODRTags`; add an nl fallback-chain assertion mirroring `testFallbackChainForPolishGoesStraightToEnglish` (`[.nl, .en]`).

- [x] 016: Update `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/HanahuacTests/AppLocaleTests.swift`: change `testAllCasesCount` assertion `AppLocale.allCases.count` 12 → 13; add `XCTAssertEqual(AppLocale.nl.id, "nl")` near the other `.id` asserts; add `testMatchingDutchDoesNotPerturbSpanish` (mirroring `testMatchingPolishDoesNotPerturbSpanish`: `nl`, `nl-NL`, `nl_NL` → `.nl`, and `es`/`es-419` still → `.esMX`); add `testDutchEnumeratedWithNativeDisplayName` (asserts `allCases.contains(.nl)`, `AppLocale.nl.displayName == "Nederlands"`, nl position after pl / before ko); add `testBundleCandidatesForDutchGoStraightToEnglish` asserting `L10n.bundleCandidates(for: .nl) == ["nl", "en"]`.

- [x] 017: Add the Dutch section to `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/HanahuacTests/L10nBundleResolutionTests.swift`, mirroring the Polish section (lines ~431-444): `testDutchBundleCandidatesGoStraightToEnglish` asserting `L10n.bundleCandidates(for: .nl) == ["nl", "en"]`, and `testDutchTranslatedKey_servedFromDutchPack` loading `ODRTestSupport.lprojBundle(for: .nl)` and asserting a representative nl key is served from the nl pack itself (COMPLETE content → no Spanish hop).

- [x] 018: Add `testProgressIsolationForDutch` to `/Users/Private/Documents/Code/ProjectHana-worktrees/additional-language-support/HanahuacTests/PerLanguageProgressTests.swift` calling `assertProgressIsolated(.nl, .en)`, mirroring `testProgressIsolationForItalian`/`testProgressIsolationForPolish`.

- [x] 019: Run full local verification with `HANA_FEATURE_SLUG=additional-language-support`: `just lint` (includes l10n-check → enforces nl is complete at 156 keys + full geo), `just test`, `just geo-packs-check`, `just verify-odr-packs`, and an iOS/Catalyst build. Fix any failures.
