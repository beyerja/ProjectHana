## Tasks
- [x] 001: In `HanahuacTests/LanguageCatalogTests.swift`, add a generic invariant test `testEveryAppLocaleHasExactlyOneDescriptor` that iterates `AppLocale.allCases` and asserts `LanguageCatalog.all.filter { $0.code == locale.rawValue }.count == 1` for each — proving a one-to-one enum↔descriptor mapping that auto-covers future languages (do NOT remove the existing per-case tests).
- [x] 002: In `HanahuacTests/LanguageCatalogTests.swift`, add `testCatalogOrderMatchesAllCasesGenerically` asserting `LanguageCatalog.all.map(\.code) == AppLocale.allCases.map(\.rawValue)` (re-affirming order generically; keep the existing `testCatalogOrderMatchesAllCases`).
- [x] 003: In `HanahuacTests/LanguageCatalogTests.swift`, add `testODRTagsAreGenericallyConsistentOverAllCases` that iterates `AppLocale.allCases` and asserts: bundled-base locales (`isBundledBaseLanguage`) have `odrTags.isEmpty`, and downloadable locales have `odrTags == ["lang-<code>"]` exactly (built from `locale.rawValue`) — so the `[lang-<code>]` convention is enforced for any future language without a hardcoded list.
- [x] 004: Add `HanahuacTests/LanguageCompletenessSupport.swift` defining an enum `LanguageCompletenessSupport` with a value type `CompletenessReport` (e.g. `missingUIKeys: Set<String>`, `geoEntitiesMissingName: Set<String>`, `countriesMissingCapital: Set<String>`). Add `static func missingUIKeys(for locale: AppLocale, baseLocale: AppLocale = .en) -> Set<String>` that compares the locale's `.lproj` `Localizable.strings` keys against the base `en.lproj` keys, resolving each `.lproj` bundle via `ODRTestSupport.lprojBundle(for:)` for downloadable packs and `L10n.bundle(for:)` for bundled-base locales; return the empty set (or skip) when the pack bundle is unreachable so it degrades like `ODRTestSupport`. Keep it dependency-free and additive so per-language stories only call it.
- [x] 005: In `HanahuacTests/LanguageCompletenessSupport.swift`, add the geo-coverage half: `static func geoCoverageGaps(for locale: AppLocale, geo: GeographyData = GeographyDataLoader.load()) -> CompletenessReport` that loads the locale's `<code>-geo.json` via `GeoNamePackLoader`/the active `LanguagePackProvider` (or `ODRTestSupport`-located pack file) and reports which `countries`/`rivers`/`mountains`/`seas` ids lack a `name`, and which countries lack a `capital`, relative to the bundled source geo. For bundled-base locales return an empty report. Drive everything off `AppLocale` + the generated pack schema so future packs are covered automatically.
- [x] 006: Add a test in `HanahuacTests/LanguageCompletenessSupport.swift` (or a sibling `LanguageCompletenessSupportTests.swift`) exercising the completeness helper on an existing language — e.g. assert `missingUIKeys(for: .en)` is empty (base vs itself) and that calling `missingUIKeys`/`geoCoverageGaps` for an existing downloadable locale (e.g. `.fr`) returns a well-formed report or skips cleanly when the pack is unreachable — satisfying AC#2 (covered by at least one test on an existing language).
- [x] 007: In `HanahuacTests/PerLanguageProgressTests.swift`, extract a reusable, locale-parameterized progress-isolation assertion helper, e.g. `private func assertProgressIsolated(_ a: AppLocale, _ b: AppLocale, file:line:)`, that seeds a card/snapshot under locale `a`, asserts locale `b`'s `CardStore`/`ProgressStatsStore` rows (keyed by `language` rawValue) stay empty, and asserts the same `factID`/day coexists — reusing the existing in-memory `ModelContainer` setup. Refactor it so per-language stories only ADD a call with their new locale; do not change existing test semantics.
- [x] 008: In `HanahuacTests/PerLanguageProgressTests.swift`, add one test that invokes the new `assertProgressIsolated` helper on an existing locale pair (e.g. `.en` vs `.ko`) to prove the helper works and satisfy AC#3 (exercised by an existing locale).
- [x] 009: Run `just -f <worktree>/justfile geo-packs-check` and confirm `scripts/generate-geo-packs.py --check` passes unchanged; do NOT modify `PACK_LANGUAGES`/`SUFFIX_BY_CODE` or any `<code>-geo.json`. This is a verification-only guard that the foundational story added no language.
- [x] 010: Verify the full gate locally: `just lint`, `just test`, `just geo-packs-check`, `just verify-odr-packs` all pass and an iOS/Catalyst build is green; confirm `AppLocale.allCases.count == 6` is unchanged (the existing `testCatalogContainsExactlySixLanguages` still passes). Fix any compile/lint issues in the new helpers/tests.
