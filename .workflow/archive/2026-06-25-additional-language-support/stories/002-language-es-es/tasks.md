## Tasks
- [x] 001: Add `case esES = "es-ES"` to `AppLocale` (Hanahuac/L10n/AppLocale.swift) immediately after `esMX`, so the enum order is en, fr, de, es-MX, es-ES, ko, nah. Do NOT touch `matching(_:)`: the `if language == "es" { return .esMX }` branch must stay, so every es-* device locale still resolves to `.esMX`.
- [x] 002: Add the `es-ES` `LanguageDescriptor` to `LanguageCatalog.all` (Hanahuac/L10n/LanguageCatalog.swift) in the same position as in `AppLocale.allCases` (right after the `esMX` entry): displayName "Español (España)", fallbackChain `[.esES, .esMX, .en]`, availability `.downloadablePack` (odrTags derive as `[lang-es-ES]` automatically). Update the file's order comment to include es-ES.
- [x] 003: Create `Hanahuac/es-ES.lproj/Localizable.strings` — NEW. Copy the full key set from `en.lproj/Localizable.strings` (~85 keys, every key including the `%d`/`%@`/`%1$@` format keys) and translate every value into real peninsular Castilian Spanish (Spain spelling/vocabulary), NOT a copy of es-MX. Preserve all format specifiers and the comment grouping headers.
- [x] 004: Add `name_es_es` + `capital_es_es` to EVERY country object in `Hanahuac/Resources/countries.json` (complete coverage — no country left without a value), using Castilian Spanish exonyms/capitals (peninsular, distinct from `name_es`/`capital_es` where Spain usage differs).
- [x] 005: Add `name_es_es` to EVERY entity in `Hanahuac/Resources/rivers.json`, `mountains.json`, and `seas.json` (complete coverage — no entity left without a value), using Castilian Spanish names.
- [x] 006: Add es-ES to the geo-pack generator (scripts/generate-geo-packs.py): append `"es-ES"` to `PACK_LANGUAGES` and `"es-ES": "es_es"` to `SUFFIX_BY_CODE`. Update the module docstring's "fr, de, ko, nah" enumeration to include es-ES.
- [x] 007: Generate `Hanahuac/Resources/es-ES-geo.json` via `just geo-packs` (do not hand-edit) and confirm `just geo-packs-check` passes (pack is up to date and well-formed).
- [x] 008: Add the es-ES ODR resources to `project.yml`: add `"Resources/es-ES-geo.json"` and `"es-ES.lproj"` to the `excludes` list, and add two tagged resource entries (`Hanahuac/es-ES.lproj` as a folder reference and `Hanahuac/Resources/es-ES-geo.json`) each with `resourceTags: [lang-es-ES]`. Mirror the existing fr/de/ko/nah block exactly. Update the surrounding explanatory comments to mention es-ES.
- [x] 009: Add `es-ES` to the ODR validation script `scripts/verify-odr-packs.sh`: extend `LANG_CODES=(fr de ko nah)` to include `es-ES` so the tag-contract, data-only, and geo-pack-freshness checks cover es-ES. Verify the `${code}.lproj`/`${code}-geo.json`/`lang-${code}` grep patterns resolve correctly for the hyphenated `es-ES` code; adjust quoting if needed. Update the header comment's tag list.
- [x] 010: Regenerate the Xcode project from project.yml via `just generate` and confirm the new es-ES `.lproj` + `es-ES-geo.json` are tagged `lang-es-ES` and excluded from the always-bundled group (check the regenerated pbxproj).
- [x] 011: Add the regression test to `HanahuacTests/AppLocaleTests.swift`: `XCTAssertEqual(AppLocale.matching(Locale(identifier: "es_ES")), .esMX)` (and `"es-ES"`) — es-ES device locale must NEVER auto-select `.esES`. Update `testAllCasesCount` (6→7), `testIdentifiable`, and `testMatchingSpanishVariants` to keep covering all cases including the new es-ES enum case while asserting it stays mapped to `.esMX`.
- [x] 012: Update `HanahuacTests/LanguageCatalogTests.swift` for the new case: bump the count assertion (6→7), add display-name (`"Español (España)"`), fallback-chain (`[.esES, .esMX, .en]`), availability (`.downloadablePack`), and ODR-tag (`["lang-es-ES"]`) expectations for `.esES`. The generic over-allCases invariants already cover it; only the hardcoded-list assertions need the new entry.
- [x] 013: Add picker native-name and fallback-resolution tests for es-ES: assert the picker enumerates `.esES` with displayName "Español (España)" (AppLocaleTests or LanguageCatalogTests) and that `L10n.bundleCandidates(for: .esES) == ["es-ES", "es-MX", "en"]`, proving the `[es-ES, es-MX, en]` chain resolves UI strings.
- [x] 014: Add completeness tests reusing story-001 helpers (`LanguageCompletenessSupport`): assert `missingUIKeys(for: .esES)` is empty (zero missing UI keys vs en.lproj) and that `geoCoverageGaps(for: .esES)` reports no missing geo names/capitals (full country/river/mountain/sea coverage), degrading via the ODR-skip path like the existing fr/de/ko/nah completeness checks.
- [x] 015: Add per-language progress isolation coverage for `.esES` in `HanahuacTests/PerLanguageProgressTests.swift` by adding one `assertProgressIsolated(.esES, .esMX)` (or `.esES`/`.en`) call using the existing reusable helper — do not modify the helper or existing test semantics.
- [x] 016: Run the full local gate and fix any failures: `just lint`, `just test`, `just geo-packs-check`, `just verify-odr-packs`, and an iOS/Catalyst build (export HANA_FEATURE_SLUG=additional-language-support). Confirm es-ES is NOT in the always-bundled group and all acceptance criteria checks are green.
