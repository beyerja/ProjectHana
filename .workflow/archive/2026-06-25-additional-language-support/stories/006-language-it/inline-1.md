Non-blocking nit: the two loops here check `fr de es-ES it ko nah` but omit `ca`, `eu`, and `yua`, which the ODR contract (`verify-odr-packs.sh` → `fr de es-ES ca eu yua it ko nah`) and `project.yml` both tag. So this base-only guard never asserts that `lang-ca` / `lang-eu` / `lang-yua` are declared on-demand. `it` was added consistently with the existing (already-incomplete) list, so this is a pre-existing coverage gap, not a regression introduced by this PR — but it would be cleaner to bring both loops in sync with the full non-base set. Not blocking.
