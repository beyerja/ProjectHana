## Tasks

- [x] 001: Add `case it` to `AppLocale` in `Hanahuac/L10n/AppLocale.swift`, placed after `case yua` and before `case ko` to match the catalog/allCases order. No change to `matching(_:)` is needed (code `it` == rawValue resolves via the existing catalog code-lookup); verify in the doc comment that the `es-*` → `.esMX` mapping is unaffected.
- [x] 002: Add the Italian `LanguageDescriptor` to `LanguageCatalog.all` in `Hanahuac/L10n/LanguageCatalog.swift`, positioned after the `yua` entry and before `ko` (matching `AppLocale.allCases`): code `AppLocale.it.rawValue`, displayName `"Italiano"`, fallbackChain `[.it, .en]` (COMPLETE-content — routes straight to English as an ultimate, never-hit safety net; NOT through es-MX/es-ES), availability `.downloadablePack`, odrTags `[lang-it]`. Update the ordering comment in the `all` doc-comment to include `it`.
- [x] 003: Add `name_it` / `capital_it` optional fields to the four geo models — `Hanahuac/Models/Country.swift` (both `name_it` + `capital_it`), `Hanahuac/Models/River.swift`, `Hanahuac/Models/MountainRange.swift`, `Hanahuac/Models/Sea.swift` (`name_it` only) — as `let nameIt: String?` / `let capitalIt: String?`, including the explicit memberwise-init parameters (defaulting to `nil`) so existing call sites stay source-compatible. Mirror the placement of the `nameEsEs`/`nameCa`/`nameYua` fields.
- [x] 004: Audit existing tests that construct `Country`/`River`/`MountainRange`/`Sea` and confirm the new `nameIt`/`capitalIt` parameters' `nil` defaults keep them compiling — grep `HanahuacTests` for `Country(`, `River(`, `MountainRange(`, `Sea(` and update any positional-arg construction broken by the added fields. (These are plain `Codable` structs, not SwiftData `@Model`s, but the init signature changed.)
- [x] 005: Add the `it` arm to every `rawName(for:)` / `rawCapital(for:)` switch in `Hanahuac/L10n/Packs/GeoModel+PackData.swift` (`case .it: nameIt` for Country/River/MountainRange/Sea, plus `case .it: capitalIt` for Country's `rawCapital`), keeping exhaustiveness for the new enum case.
- [x] 006: Create `Hanahuac/it.lproj/Localizable.strings` with COMPLETE professional Italian translations for the full canonical UI key set (all 156 keys present in `Hanahuac/en.lproj/Localizable.strings`) — zero missing keys vs en. Use real, idiomatic Italian (not machine-stub copies of English); legitimately-shared tokens (brand names like iCloud, "Asia"/"Europe" proper nouns, the `%d / 3` format string) are the only values allowed to read identically to English.
- [x] 007: Seed Italian geo content: add `name_it` (+ `capital_it` for countries) to EVERY entity in `Hanahuac/Resources/countries.json`, `rivers.json`, `mountains.json`, `seas.json` — full coverage, zero gaps (completeness is enforced, fallback to en must never be hit). Use established Italian exonyms where they exist (Germania, Spagna, Cina, Mosca, Londra, Mar Mediterraneo, Alpi) and local/native names otherwise. Prefer a committed idempotent seeding script under `scripts/` (mirroring `scripts/seed-es-es-geo.py`) invoked once, inserting the `it` columns adjacent to the existing per-language columns for readable diffs.
- [x] 008: Wire Italian into the geo-pack generator `scripts/generate-geo-packs.py`: add `"it"` to `PACK_LANGUAGES` and `"it": "it"` to `SUFFIX_BY_CODE`. Update the module docstring's language enumeration to include `it`.
- [x] 009: Generate `Hanahuac/Resources/it-geo.json` by running `just geo-packs`, and confirm it is stable via `just geo-packs-check`.
- [x] 010: Register Italian ODR resources in `project.yml`: add `it.lproj` and `Resources/it-geo.json` to the `excludes` list, and add the two `sources` entries (`Hanahuac/it.lproj` and `Hanahuac/Resources/it-geo.json`, each `resourceTags: [lang-it]`), positioned after the `yua` block and before `ko` to keep ordering consistent. Update the surrounding comments that enumerate the tagged languages to include `it`. Regenerate the project via `just generate`.
- [x] 011: Add `"it"` to the COMPLETE/full locale list `FULL_LOCALES` in `scripts/check-l10n-completeness.py` (alongside `de`, `fr`, `es-ES`, `ko`) so Italian is held to the full canonical key set. Update the module docstring's enumeration of fully-translated locales; add any genuinely-shared Italian strings (only if needed) to `IDENTICAL_VALUE_ALLOWLIST`.
- [x] 012: Add `it` to the hardcoded language-code lists in the ODR verification scripts: `LANG_CODES=(fr de es-ES ca eu yua ko nah)` in `scripts/verify-odr-packs.sh` and the `fr de es-ES ko nah` exclusion loops in `scripts/verify-base-only-bundle.sh` (Italian is a COMPLETE downloadable pack that must NOT be in the always-bundled bundle). Update the enumerating comments in both scripts.
- [x] 013: Update the hardcoded catalog-count assertions: `testCatalogContainsExactlyTenLanguages` (`LanguageCatalog.all.count == 10` → 11) in `HanahuacTests/LanguageCatalogTests.swift`, and `AppLocale.allCases.count == 10` → 11 in `HanahuacTests/AppLocaleTests.swift`. Rename the catalog-count test to reflect 11.
- [x] 014: Add Italian catalog/enum tests mirroring the es-ES/ca pattern: in `HanahuacTests/LanguageCatalogTests.swift` assert `descriptor(for: .it).displayName == "Italiano"`, `fallbackChain == [.it, .en]`, `AppLocale.it.odrTags == ["lang-it"]`, and include `.it` in the downloadable-pack iteration list. In `HanahuacTests/AppLocaleTests.swift` assert `AppLocale.it.id == "it"`, `allCases.contains(.it)`, `displayName == "Italiano"`, and that `matching(Locale(identifier: "it"))`/`"it-IT"`/`"it_IT"` resolve to `.it` without perturbing the es-* mapping.
- [x] 015: Add Italian bundle-resolution tests to `HanahuacTests/L10nBundleResolutionTests.swift`: assert `L10n.bundleCandidates(for: .it) == ["it", "en"]` (COMPLETE language — straight to en, no Spanish hop). Because Italian leaves no intentional gaps, do NOT add a gap-key fallback test; instead assert a representative `it` key is served from the it pack itself.
- [x] 016: Add `testProgressIsolationForItalian` to `HanahuacTests/PerLanguageProgressTests.swift` using `assertProgressIsolated(.it, .en)` — Italian keeps a progress track isolated from its English fallback target.
- [x] 017: Run the full gate — `just lint` (includes l10n-check), `just test`, `just geo-packs-check`, `just verify-odr-packs`, and an iOS/Catalyst build — and fix any failures. Confirm `it.lproj` + `it-geo.json` are tagged `[lang-it]` and excluded from the bundle, the completeness check passes with zero missing UI keys and full geo coverage, and `it` is not bundled.
