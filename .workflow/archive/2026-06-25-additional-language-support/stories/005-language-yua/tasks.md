## Tasks

- [ ] 001: Add `case yua` to `Hanahuac/L10n/AppLocale.swift` immediately after `case eu` (so `AppLocale.allCases` order is en, fr, de, es-MX, es-ES, ca, eu, yua, ko, nah). Do NOT touch the `es == "es"` branch in `matching(_:)` — `yua` auto-detects only via the existing catalog code-lookup path (code `yua` == rawValue) and must NOT perturb the es-* → .esMX mapping. Update the `matching(_:)` doc comment to mention `yua` alongside ca/eu as a catalog-code auto-detect.
- [ ] 002: Add the `yua` `LanguageDescriptor` to `Hanahuac/L10n/LanguageCatalog.swift` immediately after the `eu` entry: code `AppLocale.yua.rawValue`, displayName `"Màaya t'àan"`, fallbackChain `[.yua, .esMX, .en]` (routes through es-MX, NOT es-ES), availability `.downloadablePack`, odrTags via the descriptor's tag derivation (yields `["lang-yua"]`). Add a code comment noting yua is best-effort/fallback-permitted and routes through es-MX (matching `fallsBackThroughSpanish`). Keep catalog order matched to `AppLocale.allCases`.
- [ ] 003: Create `Hanahuac/yua.lproj/Localizable.strings` — copy `Hanahuac/en.lproj/Localizable.strings` as the key set and author professional Yucatec Maya values for the ~85 keys to the extent feasible. Genuine gaps may be omitted (they fall back through es-MX → en); deliberately omit at least one key that es-MX DOES translate so the fallback-chain test in task 008 has a real gap to exercise. Do NOT add yua to `scripts/check-l10n-completeness.py` FULL/PARTIAL lists (best-effort languages ca/eu are intentionally absent there; follow that precedent).
- [ ] 004: Add `name_yua` + `capital_yua` columns to `Hanahuac/Resources/countries.json` (best-effort coverage), and `name_yua` to `Hanahuac/Resources/rivers.json`, `mountains.json`, and `seas.json` (best-effort). Omit entries with no genuine Yucatec Maya form rather than copying Spanish/English; missing entries fall back through es-MX → en. Preserve existing JSON ordering/formatting.
- [ ] 005: Extend `scripts/generate-geo-packs.py`: append `"yua"` to `PACK_LANGUAGES` and add `"yua": "yua"` to `SUFFIX_BY_CODE`. Update the module docstring's language list to include `yua`.
- [ ] 006: Generate `Hanahuac/Resources/yua-geo.json` by running `just geo-packs`; verify it is up to date with `just geo-packs-check`.
- [ ] 007: Wire ODR packaging in `project.yml`: add `yua.lproj` and `Resources/yua-geo.json` to `excludes`; add both as resources tagged `[lang-yua]` (folder reference for `yua.lproj`, file for `yua-geo.json`) following the `ca`/`eu` entries. Regenerate the Xcode project with `just generate`.
- [ ] 008: Add a fallback-chain test for yua in `HanahuacTests/L10nBundleResolutionTests.swift`, mirroring the Catalan gap-key test but routing through es-MX: assert `L10n.bundleCandidates(for: .yua) == ["yua", "es-MX", "en"]`, and that a key yua OMITS but es-MX translates resolves to the es-MX value BEFORE en (build throwaway yua + es-MX `.lproj` bundles, assert the gap resolves to the Spanish value, not English). Also assert a key yua DOES translate is served from the yua pack itself.
- [ ] 009: Extend catalog/enum invariant tests in `HanahuacTests/AppLocaleTests.swift` and `HanahuacTests/LanguageCatalogTests.swift`: assert `AppLocale.yua.id == "yua"`, `displayName == "Màaya t'àan"`, catalog order (`yua` immediately follows `eu`), `descriptor(for: .yua).fallbackChain == [.yua, .esMX, .en]`, `availability == .downloadablePack`, `odrTags == ["lang-yua"]`, `AppLocale.yua.fallsBackThroughSpanish == true` (routes through es-MX), and a device-locale auto-detect case (`AppLocale.matching(Locale(identifier: "yua")) == .yua`) plus a regression that `es_ES`/`es-ES` still map to `.esMX`.
- [ ] 010: Add a per-language progress isolation test for `.yua` in `HanahuacTests/PerLanguageProgressTests.swift` using the existing `assertProgressIsolated` helper (e.g. `assertProgressIsolated(.yua, .esMX)`), confirming ReviewCard/DailyProgressSnapshot rows for `.yua` do not bleed into other locales.
- [ ] 011: Run the full gate — `just lint`, `just test`, `just geo-packs-check`, `just verify-odr-packs` (and `just verify-base-only` to confirm yua is NOT bundled), and an iOS/Catalyst build (`just build-mac`). Fix any failures. Confirm `yua.lproj` + `yua-geo.json` are excluded from the base bundle and tagged `[lang-yua]`.
