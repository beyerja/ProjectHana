## Tasks

- [x] 001: Add `case eu` to `AppLocale` in `Hanahuac/L10n/AppLocale.swift`, ordered immediately after `case ca` (matching the catalog/picker order). Update the `matching(_:)` doc comment to note `eu` auto-detects via the catalog code-lookup (code `eu` == rawValue). Confirm NO special-case edit to `matching(_:)` is needed: the existing `es == .esMX` branch and the catalog code-lookup already handle `eu` correctly without perturbing es-* mapping.
- [x] 002: Add the `eu` descriptor to `LanguageCatalog.all` in `Hanahuac/L10n/LanguageCatalog.swift`, placed immediately after the `ca` descriptor (displayName "Euskara", fallbackChain `[.eu, .esES, .en]`, availability `.downloadablePack`, no explicit odrTags arg so the default `[lang-eu]` is derived). Update the `all` doc comment's language list to include `eu`.
- [x] 003: Create `Hanahuac/eu.lproj/Localizable.strings` — professional Basque (Euskara) for the ~85 UI keys, copying `en.lproj` as the key set and translating values. Best-effort: deliberately OMIT at least one key (the eu-gap key, see task 010) so it falls back through es-ES; genuine gaps may be omitted to fall back through es-ES -> en. Mirror the structure/header style of `ca.lproj/Localizable.strings`.
- [x] 004: Add `name_eu` + `capital_eu` columns to `Hanahuac/Resources/countries.json` (best-effort professional Basque coverage; mirror how `name_ca`/`capital_ca` were added). Genuine gaps may be left absent to fall back.
- [x] 005: Add `name_eu` columns to `Hanahuac/Resources/rivers.json`, `mountains.json`, and `seas.json` (best-effort; mirror `name_ca`). Genuine gaps may be left absent.
- [x] 006: Extend `scripts/generate-geo-packs.py`: add `"eu"` to `PACK_LANGUAGES` (after `"ca"`) and `"eu": "eu"` to `SUFFIX_BY_CODE`. Update the module docstring's language list (lines mentioning "fr, de, es-ES, ca, ko, nah") to include `eu`.
- [x] 007: Generate `Hanahuac/Resources/eu-geo.json` by running `just geo-packs`, then verify with `just geo-packs-check` (must report up to date). Commit the generated file.
- [x] 008: Wire `eu` into `project.yml` ODR config: add `Resources/eu-geo.json` and `eu.lproj` to the `Hanahuac` source `excludes` block (after the `ca` entries), and add the two ODR-tagged resource entries (`Hanahuac/eu.lproj` as `type: folder` and `Hanahuac/Resources/eu-geo.json`, both `buildPhase: resources`, `resourceTags: [lang-eu]`) after the `ca` resource entries. Update the explanatory comments that enumerate "fr, de, es-ES, ca, ko, nah" to include `eu`. Regenerate the project with `just generate`.
- [x] 009: Update enum/catalog invariant tests for the new count and `eu` metadata in `HanahuacTests/AppLocaleTests.swift` and `HanahuacTests/LanguageCatalogTests.swift`: bump counts (allCases == 9, catalog == 9), add `eu` to id/displayName assertions, add `eu` to the downloadable-pack list, add an `AppLocale.eu.displayName == "Euskara"` picker-native-name test placed immediately after `ca` (assert `eu` immediately follows `ca` in `allCases`), and add `L10n.bundleCandidates(for: .eu) == ["eu", "es-ES", "en"]` and `LanguageCatalog.descriptor(for: .eu).fallbackChain == [.eu, .esES, .en]` assertions. Mirror the existing `ca` cases.
- [x] 010: Add the eu device-locale + fallback-chain regression tests in `HanahuacTests/AppLocaleTests.swift` and `HanahuacTests/L10nBundleResolutionTests.swift`. (a) `testMatchingBasqueDoesNotPerturbSpanish`: `AppLocale.matching(Locale(identifier: "eu"))` (and `eu-ES`/`eu_ES`) == `.eu`, while `es_ES`/`es-ES` still == `.esMX`. (b) Mirror the Catalan-chain tests for eu: a deliberately-omitted eu UI key (the eu-gap key from task 003) resolves to the **es-ES** value BEFORE `en` — assert chain order through Spanish (build throwaway `eu.lproj`/`es-ES.lproj` bundles via the existing `makeStringsBundle` helper pattern, route through a provider, assert resolved == es-ES value and != en value and != raw key). Add a content guard reading the shipped eu / es-ES `.lproj` from their asset packs (via `ODRTestSupport`) asserting eu omits the gap key while es-ES defines it.
- [x] 011: Add a per-language progress isolation test for `.eu` in `HanahuacTests/PerLanguageProgressTests.swift`: `testProgressIsolationForBasque` calling `assertProgressIsolated(.eu, .esES)` (mirror `testProgressIsolationForCatalan`). Do not change the shared helper.
- [x] 012: Confirm `eu` needs NO change to the static completeness gate `scripts/check-l10n-completeness.py` (best-effort downloadable locales like `ca`/`eu` are intentionally absent from `ALL_LOCALES` and are not enforced as complete). Verify `eu` is NOT added to `FULL_LOCALES`. No code change expected — this is a verification task documenting the decision in the story log.
- [x] 013: Run the full local check suite and fix any failures: `just lint`, `just test`, `just geo-packs-check`, `just verify-odr-packs`, and an iOS/Catalyst build (`just build-mac`). Confirm `eu.lproj` + `eu-geo.json` exist, are tagged `[lang-eu]`, and are excluded from the bundle (not newly bundled).
