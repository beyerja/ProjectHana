## Tasks
- [x] 001: Add `LanguageDescriptor` model in `Hanahuac/L10n/` — a value type holding `code` (matches `AppLocale.rawValue`), native `displayName`, the ordered `fallbackChain: [AppLocale]` (selected → es-MX → en for ko/nah; selected → en for fr/de; en → [en]; es-MX → [es-MX, en]), and an `availability` flag distinguishing bundled base languages (en, es-MX) from downloadable pack languages (fr, de, ko, nah). Pure data type, no logic. Document each field.
- [x] 002: Add `LanguageCatalog` in `Hanahuac/L10n/` as the single source of truth — a static ordered collection of `LanguageDescriptor` for the existing 6 languages (en, fr, de, es-MX, ko, nah) in the current `AppLocale.allCases` order, plus a `descriptor(for: AppLocale)` lookup. Encode display names and fallback chains exactly matching the current `switch` behavior.
- [x] 003: Back `AppLocale.displayName` with `LanguageCatalog` — replace the per-case `switch` (lines 16-25 of `AppLocale.swift`) with a lookup into the catalog descriptor. Preserve exact strings (English, Français, Deutsch, Español (México), 한국어, Nāhuatl).
- [x] 004: Derive the fallback chain from catalog data instead of the `fallsBackThroughSpanish` boolean switch — add a catalog-backed accessor (e.g. `AppLocale.fallbackChain` or a `bundleCandidates`-equivalent) and update `L10n.bundleCandidates(for:)` (L10n.swift lines 56-61) to read the ordered chain from the descriptor. Keep `fallsBackThroughSpanish` only if needed for compatibility, derived from the catalog; otherwise remove it and update `L10n`. Output of `bundleCandidates` must stay byte-identical: ["ko","es-MX","en"], ["nah","es-MX","en"], ["fr","en"], ["de","en"], ["es-MX","en"], ["en","en"].
- [x] 005: Back `AppLocale.matching(_:)` with catalog data — keep the special-case rules (any `es-*` → esMX; Nahuatl ISO-639-3 set nah/nhn/nch/ncj/ngu/nhe → nah; default → en) but resolve the simple per-language-code cases (en, fr, de, ko) via the catalog's `code` lookup rather than an inline switch, so adding a catalog entry suffices. Behavior must be unchanged.
- [x] 006: Expose the bundled-vs-downloadable flag on `AppLocale` (catalog-backed accessor, e.g. `isBundledBaseLanguage` / `availability`) for consumption by later stories. Defined here, unused by current call sites — verify it does not change current behavior.
- [x] 007: Add `LanguageCatalogTests` in `HanahuacTests/` covering: descriptor lookup for all 6 codes, display-name correctness, fallback-chain derivation per language (incl. en→[en], es-MX→[es-MX,en]), and the bundled/downloadable flag per language. Confirm catalog count is 6 and ordering matches `AppLocale.allCases`.
- [x] 008: Verify existing `AppLocaleTests` and `LanguageManagerTests` still pass unchanged in behavior; adjust only references that moved (e.g. `fallsBackThroughSpanish` if renamed/removed) without weakening any assertion. Run the test suite to confirm no behavior change is visible.
