## Tasks
- [x] 001: Add an active-provider accessor (`LanguagePackResolver` or `LanguagePackProvider.active`) — a single, app-wide, swappable holder of the current `LanguagePackProvider`, defaulting to `BundledLanguagePackProvider()`. This is the one seam every call site routes through; nothing branches on the concrete provider type. Make it overridable for tests (e.g. `static var current` settable, or DI through `LanguageManager`).
- [x] 002: Add geo-id-keyed name/capital resolution that walks the fallback chain across PROVIDER pack data. Extend `GeoNameResolver` (or add a sibling) with a method that, for a given geo `id` + `AppLocale`, walks `locale.fallbackChain`, asks the active provider's `geoNameData(for:)` per code, returns the first non-empty `entries[id].name`/`.capital`, and falls back to the supplied base (English) value. Keep the existing `resolve(_:byCode:base:)` for the model-field path so behavior is unchanged when pack data is absent.
- [x] 003: Route `Country.localizedName/localizedCapital` (and `River`/`MountainRange`/`Sea` `localizedName`) through the active provider via the new resolver from 002 — looking up by `id` + locale through the provider's pack data, with the model's own base (`name`/`capital`) as the final fallback. Remove reliance on the per-instance `namesByCode`/`capitalsByCode` switch as the primary path (it may remain only as the bundled provider's data source). No call site reads `nameFr`/`capitalKo`/… anymore.
- [x] 004: Handle a missing/absent pack and a parse/validation failure safely in the geo path — when the active provider returns `nil` from `geoNameData(for:)` (pack not downloaded) or the pack omits the id, resolution transparently falls through the chain (selected → es-MX for ko/nah → en) to the bundled base name; never returns a broken value or crashes. (Validation failure already yields `nil` via `GeoNamePackLoader`; ensure the resolver treats `nil` as fall-through.)
- [x] 005: Route `L10n.string(_:locale:)` through the active provider's resolved string bundle(s) instead of opening `Bundle.main` `.lproj` directly. Preserve the per-key fallback chain (selected → es-MX for ko/nah → en) and the final raw-key fallback. Use the provider's `stringBundle(for:)` per candidate code in `bundleCandidates(for:)`; keep `bundle(for:)`/`bundleCandidates(for:)` intact for the bundled provider to back.
- [x] 006: Update geo localizedName/localizedCapital call sites to confirm they resolve through the seam — verify the quiz sessions/views (`MultipleChoiceSession`, `TextQuizSession`, `MapQuizView`, `MapLearningQuizView`, `NameFeatureQuizView`, `MultipleChoiceQuizView`) still compile and produce localized names now that resolution flows through the active provider; adjust only if a signature changed.
- [x] 007: Update existing tests for the new resolution path — `LocalizedGeoNameTests`, `L10nBundleResolutionTests`, `LocalizedQuizPromptTests`, `NameFeatureQuizTests`, and `BundledLanguagePackProviderTests`: ensure they pass against provider-routed resolution. Where a test constructs a geo model and expects field-based resolution, point it at the bundled provider (built from its own fixtures) so behavior is identical.
- [x] 008: Add new tests covering resolution-via-provider and missing-pack fallback — (a) geo name/capital resolves from a STUB provider's pack data (not the model fields), proving the seam is the path; (b) when the stub provider reports a pack absent (`geoNameData` returns `nil` / `state == .notDownloaded`), geo and L10n resolution fall through the chain to the bundled base; (c) a malformed/invalid pack degrades to bundled fallback (no crash, no broken key); (d) no call site branches on the concrete provider type. (See `ProviderRoutedResolutionTests`.)
- [x] 009: Add an audit-and-reset task — grep the whole project for direct reads of `nameFr`/`nameDe`/`nameEs`/`nameKo`/`nameNah`/`capitalFr`/… and direct `Bundle.main.path(forResource:ofType:"lproj")` usage outside the bundled provider, to confirm every resolution call site now goes through the active provider seam; remove or reroute any stragglers. AUDIT RESULT: the only remaining per-language-field reads are model `init` assignments, the bundled provider's `rawName`/`rawCapital` data-source helpers (`GeoModel+PackData.swift`), and tests that intentionally assert pack data matches model fields; the only `Bundle.main.path(...lproj)` is in `L10n.bundle(for:)`, which the bundled provider's `stringBundle(for:)` backs. No production resolution call site reads the fields or opens an `.lproj` directly.
