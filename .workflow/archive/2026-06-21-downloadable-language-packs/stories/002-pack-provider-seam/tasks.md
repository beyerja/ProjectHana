## Tasks
- [x] 001: Audit existing extensions/usages of `Country`, `River`, `MountainRange`, `Sea` — grep `Hanahuac/` and `HanahuacTests/` for `extension Country|River|MountainRange|Sea`, for direct reads of the per-language fields (`nameFr`, `nameDe`, `nameEs`, `nameKo`, `nameNah`, `capitalFr`…`capitalNah`), and for every call site of `localizedName(for:)` / `localizedCapital(for:)`. Record which fields/methods must keep working so the model refactor (later tasks) preserves source/test compatibility. (Note: `GeographyDataLoaderTests` reads `country.nameNah`, `germany.nameEs`, etc., and `LocalizedGeoNameTests` constructs all four models with the full per-language argument list — these constrain the redesign.)
- [x] 002: Define `GeoNamePackData`, the versioned, DATA-ONLY pack-data model for one language's geo-name translations: a `Codable` struct carrying a schema `version: Int`, the language `code: String`, and a `[geoID: GeoNameEntry]` map where `GeoNameEntry` holds `name: String?` and optional `capital: String?` (capital only meaningful for countries). Document that it is JSON-only and never executable. New file under `Hanahuac/L10n/Packs/` (create the directory).
- [x] 003: Add a schema validator + safe loader for `GeoNamePackData`: a function that decodes JSON `Data` and validates (supported `version`, non-empty `code`, well-formed entries), returning a `Result`/throwing API. A parse or validation failure must degrade safely (return `nil` / throw a typed error the provider can catch) and NEVER `fatalError` — contrast with `GeographyDataLoader.loadJSON`'s current `fatalError`. New file under `Hanahuac/L10n/Packs/`.
- [x] 004: Define the `LanguagePackProvider` protocol (the feature's seam) under `Hanahuac/L10n/Packs/`: given an `AppLocale`/language code it exposes (a) the resolved string-lookup source for UI strings (e.g. a `Bundle` or string-table reference, mirroring `L10n.bundle(for:)`), (b) the geo-name translation data (`GeoNamePackData?`) for that language, and (c) the pack availability/state so call sites never branch on bundled-vs-ODR-vs-CDN. Document in the protocol doc-comment how the future signed-CDN provider (Ed25519 + SHA-256 + pinned key + zip-slip-safe extraction) plugs in here without changing call sites. NO network/crypto code in this story.
- [x] 005: Implement `BundledLanguagePackProvider` conforming to `LanguagePackProvider`, sourcing UI strings from the base-language `.lproj` (reusing `L10n.bundle(for:)`/`bundleCandidates(for:)`) and geo-name data from the geo JSON that already ships in the app. It must build `GeoNamePackData` per language from the existing bundled `countries.json`/`rivers.json`/`mountains.json`/`seas.json`, so the app is fully usable with ZERO downloaded packs. On any pack-data load/validation failure it falls back to the bundled English/base names and never crashes. New file under `Hanahuac/L10n/Packs/`.
- [x] 006: Refactor `Country`, `River`, `MountainRange`, `Sea` so `localizedName(for:)` / `localizedCapital(for:)` no longer use a per-locale `switch` over hardcoded `nameFr`/`capitalKo`/… fields, but resolve the localized name from the active language's pack data (via the bundled provider) keyed by language code, walking the locale's `fallbackChain`. Keep the methods' signatures and behavior identical so existing tests stay green. Per the spec, full provider wiring lands in story 003 — here establish the data path and a default resolution that uses the bundled provider. Preserve backward-compatible access to the existing fields as needed by task 001's audit (or migrate the dependent tests in the next task).
- [x] 007: Update tests that construct the geo models or read their per-language fields for the new mechanism: reconcile `HanahuacTests/GeographyDataLoaderTests.swift` (reads `nameNah`, `nameEs`, etc. and asserts ko/nah coverage) and `HanahuacTests/LocalizedGeoNameTests.swift` (constructs all four models with the full per-language argument list) so they exercise pack-data-backed resolution. Existing assertions about resolved `localizedName`/`localizedCapital` outputs (the fallback-chain expectations) must continue to hold.
- [x] 008: Add new tests under `HanahuacTests/` covering: (a) `BundledLanguagePackProvider` returns the correct string source and geo-name data per language and works with zero downloaded packs; (b) `GeoNamePackData` schema validation accepts a well-formed pack and a malformed/unsupported-version pack degrades to the bundled fallback without crashing; (c) geo-name resolution via pack data matches the previous per-field switch outputs across the en/fr/de/es-MX/ko/nah fallback chains.
- [x] 009: Register all new source files and the new `Hanahuac/L10n/Packs/` group in the Xcode project (`project.pbxproj`) for the `Hanahuac` target, and any new test files for the `HanahuacTests` target; then run `just` checks/build+tests to confirm the whole story builds green with the bundled provider and all existing + new tests pass.
