## Tasks
- [x] 001: Audit existing presentation helpers on LanguagePackDownloadState / LanguagePackState / AppLocale — grep "extension AppLocale", "extension LanguagePackDownloadState", "mappedToPackState" across the project and note any computed property (icon name, label, progress projection) the picker must reuse rather than re-declare. Record findings in the story log.
- [x] 002: Add a `LanguagePackRowState` presentation enum (or equivalent view-model row model) that projects `LanguagePackDownloadState` (plus AppLocale.isBundledBaseLanguage) into the picker's render cases: bundledAvailable, available, downloadable(notRequested), downloading(progress:), failed(retryable:). Keep it a pure mapping with no SwiftUI dependency so it is unit-testable; reuse `mappedToPackState` where appropriate, do not duplicate the state machine.
- [x] 003: Add a `LanguagePickerViewModel` (@Observable @MainActor) that enumerates languages from `LanguageCatalog.all` (mapped to AppLocale), reads per-language state from the shared `LanguagePackDownloadStore`, and exposes a row model (task 002) per language plus the current selection from `LanguageManager`. Inject the store and language manager (default to `.shared`) for testability.
- [x] 004: Wire selection + download triggering through the view model: selecting a language sets `languageManager.current` (whose didSet calls `LanguagePackProviderHolder.requestDownloadIfNeeded`), and expose a `retry(_:)` action that calls `LanguagePackProviderHolder` retry seam (`retryDownload`) for a failed downloadable language. Do NOT reimplement download logic — delegate to the story-004 holder/provider seams.
- [x] 005: Add carry-over launch reconciliation — a method (e.g. `reconcileSelectedDownloadOnAppear()` on the view model or LanguageManager) that, on picker appear / launch, re-triggers `requestDownloadIfNeeded(for:)` for the currently-selected language when it is a downloadable pack that is not yet available. This covers the case where `LanguageManager.init` restored a persisted downloadable selection without firing `current`'s didSet, so the ODR download was never kicked off.
- [x] 006: Add the new localized strings for the download UX (e.g. `settings.language.downloading`, `settings.language.download_failed`, `settings.language.retry`, `settings.language.available`) to all six `.lproj/Localizable.strings` files (en, es-MX, fr, de, ko, nah), following the existing `settings.language.*` key conventions.
- [x] 007: Rewrite `LanguagePickerView.swift` to render from the view model: native display name per language, a determinate `ProgressView(value:)` driven by the downloading fraction, a failure state with a RETRY button, a checkmark/available indicator for base + downloaded packs, and no download affordance for base languages. Attach the task-005 reconciliation to `.onAppear`/`.task`. Keep the `#Preview`.
- [x] 008: Add view-model / presentation tests (`LanguagePickerViewModelTests`) driving the picker state off a fresh `LanguagePackDownloadStore` (no live network): assert row rendering for idle/available (base + downloaded), downloading+progress (fraction surfaced), and failed+retryable (retry action present). Cover graceful offline degradation: a downloadable language whose pack is absent yields a non-crashing failed/unavailable row and selection still falls back through the chain.
- [x] 009: Add a test for the carry-over launch reconciliation (task 005): construct a LanguageManager whose persisted selection restores to a downloadable language with a `notRequested`/absent pack, install an ODRLanguagePackProvider over a fake ResourceRequestFactory + fresh store via `LanguagePackBootstrap.install`, invoke the reconcile path, and assert the download was (re-)triggered and the store transitions to `downloading` — proving didSet-on-init omission is compensated.
