## Tasks

- [x] 001: AC9 BEFORE evidence — author `.workflow/ui-walkthrough/scripts/004-map-polish.json` covering both the Map quiz (`home.mode.mapQuiz`) and the Name-Feature quiz, with `dumpTree` + `screenshot` at the overlay-prompt-card and attribution states for each. Run `just ui-walkthrough .workflow/ui-walkthrough/scripts/004-map-polish.json before` against the booted iPhone 17 / iOS 26.5 sim (HANA_FEATURE_SLUG=ship-readiness-uiux); inspect the BEFORE screenshots to confirm the baseline (no place-name labels already; overlay/attribution collision). Reference run artifacts in the story log.
- [x] 002: Audit the three quiz-map views — `Hanahuac/Views/Quiz/MapQuiz/MapQuizView.swift`, `Hanahuac/Views/Quiz/MapQuiz/MapLearningQuizView.swift`, `Hanahuac/Views/Quiz/TextQuiz/NameFeatureQuizView.swift` — and record the current divergence (prompt-card material/cornerRadius/padding, bottom-overlay padding vs. MapKit auto-attribution at the map bottom edge, Name-Feature card uses `Theme.Palette.surface`/radius 20 with no top prompt card). Produce the shared target spec (card material, corner radius, paddings, safe-area handling) the next tasks implement. No code change.
- [x] 003: Tidy the Map-quiz overlay prompt card + bottom attribution clearance in `MapQuizView.swift` — keep `.mapStyle(.imagery(elevation: .flat))` unchanged, introduce NO place-name labels. Apply the agreed shared card style and add bottom safe-area/padding so the `feedbackBanner` no longer overlaps MapKit's auto-rendered "Apple Maps / Legal" attribution at the map's bottom edge.
- [x] 004: Apply the identical overlay prompt-card style and bottom attribution clearance to `MapLearningQuizView.swift` (same satellite base, no labels), so the Map Tab learning flow matches the polished Map quiz.
- [x] 005: Align the Name-Feature quiz presentation in `NameFeatureQuizView.swift` to the shared style — reconcile the answer card material/corner-radius/padding with the Map quiz card and ensure the single-pin reference map's bottom attribution is unobstructed. Keep `.mapStyle(.imagery(elevation: .flat))`, `.disabled(true)`, and NO place-name labels.
- [x] 006: If any new user-visible string was added in tasks 003–005, add the key + value to `en.lproj/Localizable.strings` and ALL other locale `.lproj/Localizable.strings` (de, ur, nah, ar, zh-Hans, ja, eu, it, es-ES, es-MX, sr, ko, pl, pt-BR, fr, nl, bn, yua, hi, ca). If no new strings were introduced, record that explicitly and skip. Run `just l10n-check` until clean. — NO new strings introduced; skipped. `just l10n-check` PASS.
- [x] 007: If `project.yml` changed (no new files are expected for this polish pass, but verify), run `just generate`; never hand-edit the pbxproj. Otherwise note that no regeneration was needed. — No new/removed files; project.yml unchanged; no regeneration needed.
- [x] 008: Run `just lint` and `just test` (HANA_FEATURE_SLUG=ship-readiness-uiux); fix any failures introduced by tasks 003–005. — lint PASS, test TEST SUCCEEDED.
- [x] 009: AC9 AFTER evidence + verification — re-run `just ui-walkthrough .workflow/ui-walkthrough/scripts/004-map-polish.json after` against the booted iPhone 17 / iOS 26.5 sim. Inspect AFTER screenshots AND accessibility dumps to confirm: satellite `.imagery` base retained, NO place-name labels on either map, overlay card + attribution tidy and consistent across the Map and Name-Feature quizzes. Capture BEFORE/AFTER comparison and reference both run artifacts in the story/verify log.
