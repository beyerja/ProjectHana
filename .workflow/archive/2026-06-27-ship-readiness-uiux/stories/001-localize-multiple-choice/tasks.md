## Tasks

- [x] 001: Audit the MC prompt-building code in `Hanahuac/Views/Quiz/MultipleChoice/MultipleChoiceSession.swift` and confirm every prompt + every option label routes through L10n + `GeoNameResolver`. Document per category which strings are already localized (country: `quiz.prompt.capital_of` + `localizedName`/`localizedCapital`; river/mountain: `quiz.prompt.continent_of` + `localizedName`; continent options: `localizedContinent`) and which are NOT. Confirmed leaks to fix in later tasks: the SEA prompt at line ~143 is a hardcoded English string `"Which body of water is located at approximately \(region)?"` with an English `approximateRegion` (`N/S/E/W`) — not localized. Record findings in `log.md`; no code change in this task.

- [x] 002: Define the canonical NEW localization key(s) for the SEA MC prompt. Add `"quiz.prompt.sea_location" = "Which body of water is located near %@?";` to the EN base (`Hanahuac/en.lproj/Localizable.strings`) alongside the existing `quiz.prompt.*` block. If the localized region string also needs cardinal directions, add `quiz.region.north`/`quiz.region.south`/`quiz.region.east`/`quiz.region.west` keys (single-letter or short word per locale) so the coordinate label is not English-only. Keep the `%@` interpolation contract identical across all locales.

- [x] 003: Route the SEA MC prompt through L10n in `MultipleChoiceSession.seaIdentificationQuestions`: replace the hardcoded `"Which body of water is located at approximately \(region)?"` with `String(format: L10n.string("quiz.prompt.sea_location", locale: locale), region)` and make `approximateRegion(lat:lon:)` emit a locale-aware region label (use the `quiz.region.*` keys from task 002 for the cardinal letters, or pass `locale`). Ensure the sea NAME options already use `localizedName(for: locale)` (they do) and remain unchanged. No English may remain in the sea prompt path.

- [x] 004: Add the new key(s) from tasks 002–003 to EVERY required locale strings file with a natural translation: `Hanahuac/es-MX.lproj`, `Hanahuac/es-ES.lproj`, `Hanahuac/de.lproj`, `Hanahuac/fr.lproj`, `Hanahuac/it.lproj`, `Hanahuac/pl.lproj`, `Hanahuac/nl.lproj`, `Hanahuac/sr.lproj`, `Hanahuac/ko.lproj` (these are REQUIRED_LOCALES in `scripts/check-l10n-completeness.py` and must be 100% complete). Spanish must read naturally (e.g. `"¿Qué masa de agua se encuentra cerca de %@?"`). No value may be byte-identical to English unless it is a legitimately-shared token added to `IDENTICAL_VALUE_ALLOWLIST`.

- [x] 005: Add the same new key(s) to the remaining on-disk locales for consistency: `Hanahuac/eu.lproj`, `Hanahuac/ca.lproj`, `Hanahuac/yua.lproj` (present on disk; eu/ca/yua mirror es-ES). For `Hanahuac/nah.lproj` (PARTIAL_LOCALES, fallback nah -> es-MX -> en) add the key if other `quiz.prompt.*` keys are present there; otherwise leave it to fall back (nah is informational-only in l10n-check). Verify every other category prompt key (`quiz.prompt.capital_of`, `quiz.prompt.continent_of`) is already present in these files — it is for all except nah — and add any that are missing.

- [x] 006: Run `just l10n-check` (via the worktree justfile) and resolve every failure/warning: ensure the new key exists in all REQUIRED_LOCALES, and add any genuinely-shared translation to `IDENTICAL_VALUE_ALLOWLIST` in `scripts/check-l10n-completeness.py` with a one-line justification comment. Gate must exit 0.

- [x] 007: Add/extend unit coverage in `HanahuacTests` for the MC prompt localization: assert that `MultipleChoiceSession.seaIdentificationQuestions(..., locale: .es)` (and one non-Spanish locale, e.g. `.de` or `.ko`) produces a prompt that contains the localized sea name and the localized template, and contains NO English template fragment ("Which body of water"). Mirror the existing `GeoNameResolverTests`/quiz test patterns. Also assert country/river/mountain prompts stay localized as a regression guard.

- [x] 008: Author the ui-walkthrough verification script at `.workflow/ui-walkthrough/scripts/001-localize-mc.json` (JSON array of step objects per `.workflow/ui-walkthrough/README.md`). Steps: launch + initial `dumpTree`/`screenshot` (step 000 auto), navigate Home -> Settings (`tap` `identifier`=`home.settings`) -> open the language picker (`Hanahuac/Views/Settings/LanguagePickerView.swift`) and select Spanish, return Home, `tap` `identifier`=`home.mode.multipleChoice`, `wait`, `dumpTree` + `screenshot` on the country prompt; then repeat the open-MC step for at least one other category (sea) and `dumpTree` + `screenshot`. Use label-first targeting (the README's resolution order) and only identifiers that exist (`home.settings`, `home.mode.multipleChoice`, `quiz.answer.N`).

- [x] 009: Run `just generate` (xcodegen, never hand-edit pbxproj) if any new source/test files were added, then `just lint` (includes `lint-swift` + `l10n-check`) and `just test`. Fix all SwiftLint/swiftformat, l10n, and test failures until both pass cleanly. Record the green run in `log.md`.

- [x] 010: Run the walkthrough `just ui-walkthrough .workflow/ui-walkthrough/scripts/001-localize-mc.json <run>` against the booted iPhone 17 / iOS 26.5 sim, read the produced screenshots AND accessibility dumps, and confirm: the country MC prompt reads "¿Cuál es la capital de <Localized Country>?" (e.g. "Países Bajos", no English) and the sea MC prompt is fully Spanish with a localized sea name. Capture before/after evidence and reference the run-artifact paths in `log.md` / the verify log (AC9). No English leakage in any of the four categories.
