## Tasks
- [x] 001: Refactor `CapitalQuizView.quizBody` so the answer field + action button area is pinned to the bottom and stays above the keyboard. Move the answer input/button out of the trailing `Spacer()`-padded `ScrollView` flow into a `.safeAreaInset(edge: .bottom)` (or a top `ScrollView` for the prompt/progress + a pinned bottom bar) so SwiftUI keyboard-avoidance lifts the field+"Verificar" button together and the button is never pushed below the keyboard. File: `Hanahuac/Views/Quiz/TextQuiz/CapitalQuizView.swift`.
- [x] 002: Make the layout robust to the iOS multilingual-keyboard onboarding card: add `.scrollDismissesKeyboard(.interactively)` to the prompt scroll region and ensure the pinned answer/button bar respects `.ignoresSafeArea(.keyboard)` semantics correctly (the bar must ride above the keyboard inset, the scroll content must remain reachable). Verify both the `.unanswered` (field + "Verificar") and feedback (`.correct`/`.incorrect`, "Siguiente") states keep their action button hittable. File: `Hanahuac/Views/Quiz/TextQuiz/CapitalQuizView.swift`.
- [x] 003: Confirm no new user-visible strings were introduced (the change is layout-only, reusing `capital_quiz.placeholder`/`capital_quiz.check`/`capital_quiz.next`). If any new string is unavoidable, add it to ALL locale `Localizable.strings` files and run `just -f <worktree>/justfile l10n-check`.
- [x] 004: Run `just -f <worktree>/justfile generate` (only if project.yml/file membership changed — layout-only edits to an existing file need no regen), then `just -f <worktree>/justfile lint` and `just -f <worktree>/justfile test`; fix any failures.
- [x] 005: Author the AC9 live driver action script at `.workflow/ui-walkthrough/scripts/005-type-capital-layout.json`: tap `home.mode.typeCapital`, wait, dumpTree+screenshot (baseline), `typeText` into identifier `quiz.input` to raise the keyboard, wait, then dumpTree+screenshot. The dump must show both `quiz.input` and `quiz.submit` present/hittable while the keyboard is up.
- [x] 006: Capture AC9 before/after live evidence: run `just -f <worktree>/justfile ui-walkthrough .workflow/ui-walkthrough/scripts/005-type-capital-layout.json <run>` against the booted iPhone 17 / iOS 26.5 sim on the pre-fix and post-fix builds. Read the screenshots AND accessibility dumps; confirm "Verificar"/`quiz.submit` is obscured before and visible/hittable after. Reference the run artifact paths in the story/verify log (AC9).
- [x] 007: Verify all three acceptance criteria against artifacts: (AC1) field visible + accepts input with keyboard up; (AC2) "Verificar" visible/tappable and not obscured by the onboarding card; (AC3) `l10n-check`, `lint`, `test` all pass.
