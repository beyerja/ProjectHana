## Tasks

- [x] 001 (AC3): Remove the redundant manual chevron from the Settings "Idioma" row so only the NavigationLink's built-in disclosure chevron shows. Delete the manual `Image(systemName: "chevron.right")` block (and its surrounding `HStack`/`Spacer`, reverting the label to a plain `Text(L10n["settings.language"])`) in `Hanahuac/Views/Settings/SettingsView.swift` (lines ~16-23). Keep `.accessibilityIdentifier("settings.language")`.

- [x] 002 (AC4): Fix the blurry/duplicated icon in the map-learning header. In `Hanahuac/Views/Quiz/MapQuiz/MapLearningQuizView.swift`, locate the header/toolbar treatment around the `.navigationTitle(L10n["learn_map.title"])` + `.inlineNavigationTitle()` (lines ~43-44) and any custom toolbar/principal icon; ensure a single crisp element renders beside the back button (remove a duplicated/overlaid icon or replace a low-res asset with a single SF Symbol). If no custom toolbar icon exists in this file, grep for the icon source feeding this header (e.g. a shared toolbar/back-button modifier or `HomeView` route presentation) and fix it at that site. Do not introduce new localized strings unless an accessibility label is added (if so, follow task 005's all-locales rule).

- [x] 003 (AC7): Add four short, localized tier-name keys for the Progress category-table columns. Add `stats.tier.name.new`, `stats.tier.name.learning`, `stats.tier.name.review`, `stats.tier.name.mastered` to `Hanahuac/en.lproj/Localizable.strings` (English values: "New" / "Learning" / "Review" / "Mastered"), then add the same four keys with translated values to ALL 20 other locale files: ar, bn, ca, de, es-ES, es-MX, eu, fr, hi, it, ja, ko, nah, nl, pl, pt-BR, sr, ur, yua, zh-Hans (each under `Hanahuac/<locale>.lproj/Localizable.strings`). These names replace the hardcoded English `MasteryTier.rawValue` used as user-visible text.

- [x] 004 (AC7): Expose a localized tier name on the model. Add a `var localizedName: String` (or `displayName`) computed property to `MasteryTier` returning `L10n["stats.tier.name.new"]` etc. per case. NOTE — extension/collision check: a `description` computed property already exists on `MasteryTier` in `Hanahuac/Views/Progress/StatsView.swift` (lines ~290-299); add the new property without clashing with it and reuse it rather than re-declaring. Define `localizedName` either in `Hanahuac/Models/MasteryTier.swift` or alongside `description` in `StatsView.swift` (pick one site, keep consistent).

- [x] 005 (AC7): Label the Progress category-table header icons and replace hardcoded raw-value labels. In `Hanahuac/Views/Progress/StatsView.swift`: in `categoryHeader` (lines ~223-237) add `.accessibilityLabel(tier.localizedName)` to each header `Image(systemName: tier.icon)` so the accessibility dump exposes meaningful labels instead of bare icons (consider also showing the short name text under/next to each icon for a visible legend); in `tierLegendSection` (lines ~261-287) replace `Text(tier.rawValue)` with `Text(tier.localizedName)` so the visible legend is localized. Verify `just l10n-check` passes.

- [x] 006 (verification setup): Author the UI-walkthrough action script `.workflow/ui-walkthrough/scripts/003-ui-polish.json` (JSON array per `.workflow/ui-walkthrough/README.md` schema). Cover all three surfaces with `screenshot`+`dumpTree` markers: (a) Settings — tap `home.settings`, dump/screenshot the Idioma row (one chevron); (b) map-learning header — tap `home.mode.mapQuiz`, navigate to the new-card pile to push `MapLearningQuizView`, dump/screenshot the header (one crisp icon beside back button); (c) Progress table — tap `home.progress`, dump/screenshot the category table (dump exposes column labels / visible legend text). Use accessibility identifiers `home.settings`, `home.mode.mapQuiz`, `home.progress`, and `BackButton` for navigation.

- [x] 007 (AC9 + AC4 of spec / live verification): Capture BEFORE/AFTER evidence. Run `HANA_FEATURE_SLUG=ship-readiness-uiux just -f /Users/Private/Documents/Code/ProjectHana-worktrees/ship-readiness-uiux/justfile ui-walkthrough .workflow/ui-walkthrough/scripts/003-ui-polish.json <run>` against the booted iPhone 17 / iOS 26.5 simulator — once BEFORE the fixes (or use prior baseline) and once AFTER. Read the emitted screenshots AND accessibility dumps for all three surfaces and reference the run artifact directories in `log.md`/the verify log.

- [x] 008 (AC of spec #4): Run the full gate — `just lint` (includes `l10n-check`) and `just test` (via the worktree justfile with `HANA_FEATURE_SLUG=ship-readiness-uiux`); fix any failures. Confirm `just l10n-check`, `just lint`, and `just test` all pass.
