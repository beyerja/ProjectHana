## Tasks

- [x] 001: Add an automated key-completeness check script `scripts/check-l10n-completeness.py` (stdlib-only, zero deps) that parses every `Hanahuac/<code>.lproj/Localizable.strings` and asserts: (a) the union of all keys defines the canonical key set; (b) every BASE/always-bundled locale (en, es-MX) and every fully-translated downloadable locale (de, fr, ko) contains the FULL canonical key set — fail listing any missing keys; (c) nah is allowed to be a partial subset (nah->es-MX->en fallback is the established convention, see L10n.swift bundleCandidates), so report nah coverage as informational, not a failure; (d) detect untranslated values, i.e. non-base locale values byte-identical to the en value for the same key (warn-list, with an allowlist for legitimately-identical strings like brand names/symbols). Mirror the style of existing `scripts/verify-odr-packs.sh`.
- [x] 002: Wire the check into a `just l10n-check` recipe (and fold it into `just lint`), so CI runs it; verify the recipe is path/worktree-safe and exits non-zero on missing keys.
- [x] 003: Fix the confirmed base-key gap: `stats.by_mode` is used in code (Hanahuac/Views/Progress/StatsView.swift:137) and present in es-MX/de/fr/ko but MISSING from the en base `Localizable.strings`, so English users see the raw key. Add the English value to `Hanahuac/en.lproj/Localizable.strings` (place it adjacent to the other `stats.*` keys).
- [x] 004: Run the completeness check (task 001/002) and resolve every remaining missing key it reports for en/es-MX/de/fr/ko — add the missing translated value to each affected `.lproj`. Preserve the ODR folder-reference structure for fr/de/ko/nah (edit existing files only; do not add/remove `.lproj` folders in `project.yml`). RESULT: only `stats.by_mode` (en) was missing across required locales; no other gaps.
- [x] 005: Review the untranslated-value warnings from the check; for any non-base value that is genuinely still English (not a legitimate shared string), supply the correct translation in that locale's `.lproj`; add clearly-justified entries to the script's identical-string allowlist for the legitimate cases. RESULT: all flagged values are legitimate (iCloud brand, "Multiple Choice"/"Status" German loanwords, French cognates Correct/Incorrect/Question/Europe, and the "%d / 3" format-only string); all added to the allowlist with justifications. No genuine English stubs found.
- [x] 006: Audit Swift sources for hardcoded user-facing English string literals that bypass L10n. RESULT: only `Text("Hanahuac")` in HanahuacWordmark (HanahuacLogo.swift:128) — the app's brand wordmark/proper noun, intentionally NOT localized. No user-facing translatable literals found.
- [x] 007: Move every user-facing hardcoded literal found in task 006 to a localized key. RESULT: no hardcoded user-facing strings found (only the brand wordmark, which is correctly left as-is). Skipped.
- [x] 008: Add a Swift unit test `HanahuacTests/L10nCompletenessTests.swift` (matching the QuizAccessibilityStringsTests pattern) that asserts, for the always-resolvable chain, that every canonical key resolves to a non-key, non-empty value for all `AppLocale.allCases` via `L10n.string(_:locale:)`. Key list driven from the en base bundle so new keys are covered automatically.
- [x] 009: Confirm the nah fallback convention explicitly: added a focused assertion in L10nCompletenessTests that an untranslated nah key (`stats.by_mode`) resolves through nah->es-MX->en to a real value and never the raw key, documenting that missing nah keys resolving via fallback are intentional and acceptable.
- [x] 010: Verify ODR tag integrity is untouched: `just verify-odr-packs` PASSES; `project.yml` unchanged (fr/de/ko/nah still ODR-tagged, en/es-MX always-bundled); no `.lproj` folder references added/removed.
- [x] 011: Ran `just l10n-check`, `just lint`, and `just test` — all green. Audit method and results recorded in the story log.
