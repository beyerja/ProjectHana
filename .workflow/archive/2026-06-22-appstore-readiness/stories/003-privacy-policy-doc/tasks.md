## Tasks
- [x] 001: Verify privacy claims against the actual codebase BEFORE writing the policy. Grep the Swift sources for networking/tracking/permission APIs and record the findings so the doc stays factual: `URLSession`, `URLRequest`, `URLConnection`, `Network.` / `NWConnection`, `Socket`; `CLLocation` / `CoreLocation` / `MapKit` network use; `AVCapture` / `AVFoundation` camera; `PHPicker` / `Photos`; `UNUserNotification` / push; any analytics/tracking SDK names (Firebase, Crashlytics, GoogleAnalytics, Amplitude, Mixpanel, Sentry, Facebook). Confirm zero third-party SPM/Cocoapods dependencies (check `project.yml` packages + any `Package.resolved`). Confirm storage is on-device only (SwiftData/UserDefaults) and that CloudKit/iCloud sync is gated behind the undefined `CLOUDKIT_SYNC` flag (cross-check `docs/icloud-sync.md`, `Hanahuac/Sync/SyncFeatureFlag.swift`). Note any finding that would contradict a "no data collected / fully offline" claim.
- [x] 002: Author the privacy policy at `docs/privacy-policy.md`, written to be factually consistent with the task-001 findings. Required content: a clear top-level title and last-updated date (2026-06-21); a plain statement that the app (Hanahuac) collects and transmits NO personal data and is fully offline (no network/location/camera/photo/notification access); a section stating all user data (learning progress, preferences) is stored on-device only in local storage; a note that iCloud/CloudKit sync is currently disabled (gated behind the `CLOUDKIT_SYNC` compile-time flag, not active in shipped builds); a "no third-party SDKs / analytics / tracking / advertising" section; a "data sharing: none" section; and a contact line. Structure it with clear headings and clean prose so it can be hosted as a static page and referenced from App Store Connect.
- [x] 003: Verify the new doc passes lint and is internally consistent. Run `just -f /Users/Private/Documents/Code/ProjectHana-worktrees/appstore-readiness/justfile lint` (yamllint is the configured lint that touches doc-adjacent files; markdown is not separately linted but keep formatting clean). Re-read `docs/privacy-policy.md` against the task-001 findings and the spec acceptance criteria to confirm no claim contradicts the code, the last-updated date and contact line are present, and section structure is hosting-ready.
