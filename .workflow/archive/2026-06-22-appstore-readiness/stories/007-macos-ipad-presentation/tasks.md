## Tasks
- [x] 001: Baseline build verification — run `just -f <worktree>/justfile build-mac` (Mac Catalyst, ad-hoc signing) on the unmodified tree and record whether it succeeds and whether the build emits any app-icon idiom warnings (e.g. "no image found for idiom 'mac'" / unassigned-children). This establishes the concrete pre-fix state the rest of the story is traced against.
- [x] 002: Audit AppIcon asset-catalog idiom coverage — inspect `Hanahuac/Assets.xcassets/AppIcon.appiconset/Contents.json`. It currently declares one image with `"idiom": "universal"`, `"platform": "ios"`, `"size": "1024x1024"`. Confirm against task 001's build output whether Mac Catalyst is left without a usable icon idiom (the `platform: ios` key scopes the single image to iOS only). Document the exact gap; do NOT change anything yet.
- [x] 003: Fix macOS/Catalyst app-icon idiom coverage (only if task 002 confirms a gap) — update `AppIcon.appiconset/Contents.json` to a single-size config that covers Mac Catalyst, by dropping the `"platform": "ios"` restriction on the universal 1024x1024 entry (Xcode "Single Size" icon, which auto-derives all idioms incl. Mac) and/or adding the required `"mac"` idiom entries pointing at the existing `AppIcon.png`. Reuse the existing 1024 PNG; do not add new image files or generate new sizes. Keep the asset-catalog change confined to the AppIcon set.
- [x] 004: Regenerate the xcodeproj if needed and rebuild for Mac Catalyst — only AppIcon Contents.json changed (no project.yml sources/resources change), so `just generate` is likely unnecessary; run it only if XcodeGen inputs changed. Re-run `just -f <worktree>/justfile build-mac` and confirm the icon-idiom warning from task 001 is gone and the build still succeeds under ad-hoc/free signing.
- [x] 005: Verify iPad/macOS layout adaptation by inspection + build — confirm the view tree uses adaptive layout (NavigationStack + `frame(maxWidth: .infinity)` throughout; the only fixed `.frame(width:)` uses are small control/glyph sizes like 42/44/56 and GeometryReader-driven logo sizing — none impose a fixed full-screen width). Record this audit. Do NOT speculatively rework layouts; only open a follow-up fix task if the Mac Catalyst / iPad build or run surfaces a CONCRETE clipped/letterboxed defect.
- [x] 006: Build for the iPad idiom and capture findings — run a build (and run if feasible under free/Personal-Team signing, e.g. iPad simulator via `just build-sim`) targeting an iPad destination to confirm the layout renders without clipping/letterboxing. Note any concrete defect found; fix only verifiable issues. Record what was checked and the outcome for the PR description.
- [x] 007: Record verification findings for the PR — assemble the "what was checked / what was fixed" summary (icon idiom gap + fix, layout adaptation audit, Mac Catalyst + iPad build results under free signing) so create-pr can put it in the PR description, satisfying the story's verification acceptance criterion.
