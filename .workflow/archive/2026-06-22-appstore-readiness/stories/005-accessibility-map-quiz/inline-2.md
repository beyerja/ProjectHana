Note (non-blocking): the interactive pins now carry labels/value/hint, which satisfies the AC. The `featureOverlays(...)` regions remain without explicit accessibility treatment — that's acceptable since they are decorative correctness shading rather than separate interactive targets, and the result is also announced via the feedback banner. Flagging only so it's a conscious scope decision, not an oversight.
