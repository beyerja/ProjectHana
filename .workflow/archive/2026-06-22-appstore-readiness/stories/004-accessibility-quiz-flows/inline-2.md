**Non-blocking (cleanup):** `@ScaledMetric(relativeTo: .largeTitle) private var completionIconSize: CGFloat = 64` is declared identically in three views (here, `CapitalQuizView`, `NameFeatureQuizView`), with a fourth near-identical `iconSize = 64` in `QuizSummaryView`. All four drive the same completion/result glyph at the same 64pt base relative to the same text style. A small shared `ScaledIcon` view/modifier (or one shared scaled value) would keep the four in sync if the design baseline ever changes. Pure cleanup — no correctness impact.
