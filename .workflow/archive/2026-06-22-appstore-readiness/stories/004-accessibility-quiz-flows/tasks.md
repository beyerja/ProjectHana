## Tasks

- [x] 001: Add the new accessibility string keys to all 6 `.lproj/Localizable.strings` (en, es-MX, fr, de, ko, nah) under a shared `a11y.*` namespace. Cover: answer-text-field label/hint, check/next button hints, progress indicator (`"Question %d of %d"`), score/correct-count value, correct/incorrect/selected choice state words (`"correct answer"`, `"incorrect answer"`, `"selected"`, `"not answered"`), feedback announcement, and quiz-summary stat labels/accuracy value. Use the existing `L10n["key"]` pattern and `String(format:)` for parameterized strings. Provide real translations for en/es-MX/fr/de; for ko/nah follow the established convention in their in-tree `.lproj` (translate where the pack already translates the surrounding keys, otherwise let the es-MX→en fallback chain cover them — match how neighboring keys are handled). Hanahuac/{en,es-MX,fr,de,ko,nah}.lproj/Localizable.strings
- [x] 002: Add VoiceOver accessibility to CapitalQuizView quiz body — label/value/hint the progress text and correct-count as combined accessibility elements; label the prompt card; give the answer TextField an `.accessibilityLabel`/`.accessibilityHint`; label the Check and Next buttons with hints; make the correct/incorrect feedback an announced element conveying state via label (not color alone). Also replace any fixed-size font with a scalable Dynamic Type style (audit `Image(systemName:).font(.system(size: 64))` in completionView and any `.font(.system(size:))`). Hanahuac/Views/Quiz/TextQuiz/CapitalQuizView.swift
- [x] 003: Add the same VoiceOver treatment to NameFeatureQuizView text/input flow — label the `name_feature.prompt` + progress header, the answer TextField (label/hint), Check/Next buttons, and correct/incorrect feedback (state conveyed in label). The reference map pin's accessibility is OUT of scope (story 005) — do not annotate the Map/Annotation here; only the text-input chrome. Fix any fixed-size fonts. Hanahuac/Views/Quiz/TextQuiz/NameFeatureQuizView.swift
- [x] 004: Add VoiceOver accessibility to MultipleChoiceQuizView — expose each option Button as a distinct accessibility element with `.accessibilityLabel(option.label)` and convey selected/correct/incorrect state via `.accessibilityValue`/`.accessibilityAddTraits(.isSelected)` (NOT color alone), using the `buttonColor`/`session.answerState` logic as the source of truth; label the prompt card; combine the progress header into a labeled element conveying "Question X of Y" and the correct count. Fix any fixed-size fonts. Hanahuac/Views/Quiz/MultipleChoice/MultipleChoiceQuizView.swift
- [x] 005: Add VoiceOver accessibility to LearningQuizView — same option-button state treatment as task 004 (value/trait, not color), labeled prompt card, and combined progress header conveying graduated/active counts. Replace the `Image(systemName: "graduationcap.fill").font(.system(size: 64))` and any other fixed-size fonts in completionView with scalable styles; label the completion summary row and Done button. Hanahuac/Views/Quiz/LearningQuizView.swift
- [x] 006: Add VoiceOver accessibility to QuizSummaryView — combine each `statRow` into a single element reading "label: value" via `.accessibilityElement(children: .combine)` so reviewed/correct/accuracy/next-review read in a sensible order; label the "Session Complete" header and the result icon (or mark it decorative); add the accuracy percentage as a value. Replace the `Image(systemName:).font(.system(size: 64))` fixed size with a scalable style. Hanahuac/Views/Quiz/MapQuiz/QuizSummaryView.swift
- [x] 007: Add an XCTest verifying every new `a11y.*` key resolves (non-key, non-empty) for all 6 AppLocale cases through `L10n.string(_:locale:)`, following the existing pattern in L10nBundleResolutionTests.swift (use the fallback chain — assert no key surfaces the raw key for any locale). Register the new test file in project.yml only if test files are not auto-globbed; otherwise regenerate the xcodeproj per project.yml being source of truth. HanahuacTests/QuizAccessibilityStringsTests.swift
