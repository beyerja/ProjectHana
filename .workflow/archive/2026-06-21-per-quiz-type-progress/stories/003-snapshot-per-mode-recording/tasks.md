# Log — Story 003: Record daily snapshots per mode (aggregated default preserved)

## Tasks
- [x] 001: Make `ProgressStatsStore` snapshot operations quizMode-aware. Add a private `quizMode`-scoped snapshot fetch/`canonicalSnapshot`/`deduplicate` keyed on `(day, language, quizMode)`. Keep the existing public `recordSnapshot(cards:streak:date:)` writing the aggregate `(day, language, "")` row (back-compat for any non-mode caller), and the default `allSnapshots`/`snapshots(since:)` reading the aggregate (`quizMode == ""`) so the Progress screen default is unchanged.
- [x] 002: Add `recordSnapshot(allCards:modeCards:mode:streak:date:)` that upserts BOTH the aggregate `(day, language, "")` snapshot from `allCards` (cross-mode union) AND the per-mode `(day, language, mode.rawValue)` snapshot from `modeCards`. This fixes the Story-002 follow-up (aggregate stops being overwritten by one mode) and records per-mode data for the breakdown (Story 006).
- [x] 003: Add a `perModeSnapshots(since:)` (or `snapshots(for mode:since:)`) accessor returning the per-mode rows, for Story 006's breakdown. Make `deduplicate()` collapse within each `(day, quizMode)` group (not across modes).
- [x] 004: Update the 5 quiz-view call sites (`MapQuizView`, `MultipleChoiceQuizView`, `CapitalQuizView` x2, `NameFeatureQuizView`) to call the new method, passing `cardStoreProvider.allCards` (aggregate), `cardStore.allCards` (this mode), and the view's mode token. Streak handling unchanged.
- [x] 005: Audit existing `ProgressStatsStore`/snapshot tests (`ProgressStatsStoreTests`, `ProgressRecordingTests`, `PerLanguageProgressTests`, `StatsTests`) — the defaulted aggregate path must still pass; the new column is defaulted so memberwise inits compile.
- [x] 006: Tests: aggregate snapshot equals sum across modes & matches pre-feature numbers; recording mode A then mode B leaves the aggregate = union and each per-mode row independent; a graded review in any mode still advances the single per-language streak (shared, regression guard). `just generate`/`lint`/`test` green.
