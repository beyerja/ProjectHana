# Log — Story 002: Scope CardStore per quiz mode and vend a store per active mode

## Tasks
- [x] 001: Scope `CardStore` by `(language, quizMode)` — add `let quizMode: String`; add `quizMode` to every `FetchDescriptor` predicate (`allCards`, `dueCards`, `newCards`), stamp it in `seedIfNeeded`/`upsert`, and key dedup/reset by it (store already mode-scoped → group by factID is fine). Update doc comments.
- [x] 002: Add `CardStoreProvider` (`@Observable`) that lazily builds+caches one seeded `CardStore` per `HomeQuizMode` for the active language; `store(for:)`, an aggregate `allCards` union accessor (deduped by identity for StatsView), and an aggregate `revision` (sum of child revisions) for invalidation. Rebuilds on language change.
- [x] 003: Wire the provider into the app: `AppRootView.rebuildStores` builds a `CardStoreProvider` (running the migrator first, then seeding each mode's store) and injects it via `.environment`. Keep injecting a `ProgressStatsStore` (unchanged this story). Re-key subtree on language as today.
- [x] 004: Update `HomeView` to read `@Environment(CardStoreProvider.self)`; resolve the per-mode store for each row's new/pending counts (`provider.store(for: mode)`), pass the per-mode `newCards` into `directQuizView`'s learning views, and tie invalidation to `provider.revision`.
- [x] 005: Update each quiz view + `PilePickerView` to resolve their own mode's `CardStore` from the provider (replace `@Environment(CardStore.self)`): `MapQuizView`/`MapLearningQuizView` (mapQuiz), `MultipleChoiceQuizView`/`LearningQuizView` (multipleChoice), `CapitalQuizView` (typeCapital), `NameFeatureQuizView` (nameFeature), `PilePickerView` (its `mode`). For init-based views, pass the resolved store or its cards in from HomeView.
- [x] 006: Update `StatsView` to read the provider's aggregate cards (today's whole-language mastery numbers across modes) so it keeps compiling and showing the same totals; per-mode breakdown is Story 006.
- [x] 007: Update `PreviewStore.withPreviewStore()` and all `#Preview`s to vend a `CardStoreProvider` so previews compile.
- [x] 008: Tests: add cross-mode isolation test (grading/ mutating a fact's card in one mode leaves the other modes' cards for that fact untouched); `typeCapital` only seeded/queried for Countries. Update existing `CardStoreTests`/`PerLanguageProgressTests` that construct `CardStore(modelContext:language:)` for the new `quizMode` param (defaulted where possible to minimize churn). `just generate`, `just lint`, `just test` green.
