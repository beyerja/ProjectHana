# CODEOWNERS — obligatory independent review gate
#
# This assigns the entire repository to @Hanahuac-Bot as the required code owner.
#
# WARNING — committing this file DOES activate the gate in this repo. An active
# repository ruleset (`main`, id 17373423) already sets `require_code_owner_review:
# true`. That rule has no effect while no CODEOWNERS file exists, but the moment this
# file lands on `main`, every PR to `main` requires an approving review from
# @Hanahuac-Bot. Provision the bot FIRST (Write collaborator + classic PAT in the
# Keychain — see docs/bot-credentials.md), or PRs to `main` will deadlock until it can
# approve. See .github/branch-protection.md for the full picture.

* @Hanahuac-Bot
