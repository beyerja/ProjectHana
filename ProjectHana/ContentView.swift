import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("ProjectHana")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
