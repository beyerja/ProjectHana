import XCTest
@testable import ProjectHana

final class ProjectHanaTests: XCTestCase {
    func testPlaceholder() throws {
        // Stub test — real tests will be added in subsequent stories.
        XCTAssertTrue(true)
    }
}
